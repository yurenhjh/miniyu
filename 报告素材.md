# 第4组 · 可写入报告的素材汇总

> 用途：把本项目里能直接搬进课程报告/演示材料的**结论、数字、机制、真机验证、坑与边界**汇总到这里，
> 写报告时按章节摘取即可。所有条目均来自本仓库真实代码与实测，重要结论标注出处（进度跟踪 / ADR / examples）。
> 最后更新：2026-09-08

---

## 1. 一句话概括项目

在"MCP 风格的工具注册表"之上，为 Agent（LLM）提供 **OS Skills（操作系统技能服务层）**：
既能管理文件，又能像人一样**操作真实软件**（桌面 QQ 发消息、驱动 Edge 浏览器访问网页），
并且全程用**验证代替假设**（截图 + 剪贴板读回 + OCR 门 + DOM 快照），输出结构化结果给 Agent 用。

---

## 2. 数字清单（写报告直接抄）

| 指标 | 数值 | 备注 |
|------|------|------|
| 注册工具总数 | **59** | 文件管理 33 + 应用操作 7 + 浏览器 9 + 系统管理 8 + 编程开发 2（search_in_files / edit_file） |
| 技能总数 | **26** | 文件/系统 + 应用操作 + 浏览器 + QQ读/QQ发 + 邮件组合技能（Agent 白名单 5：app_send_message / read_qq_chat / send_email / browser_search / browser_extract） |
| 单元测试 | **512 个，全部通过** | `python -m pytest -q`（2026-09-08 补：本地图片直读 read_image / 编程项目辅助 search_in_files+edit_file / KaTeX 公式渲染 / 深浅主题 等用例） |
| 测试文件 | 21 个 | test_agent / test_agent_skills / test_agent_read_web / test_agent_vision / test_authz / test_email / test_agentic / test_agent_config / test_app_tools / test_conversation / test_llm_client / test_mock / test_model_switch / test_os_service_api / test_skills / test_tool_registry / test_web_http / test_tool_protocol / test_bailian_tools / test_tool_free_sse / test_stream_stop_encoding |
| 决策记录 ADR | 11 篇 | `docs/adr/0001~0011`，覆盖工具契约与关键重构 |
| Mock 对等 | 59 工具 / 26 技能 | 带 `[MOCK]` 标记，供联调/CI 无真机跑 |
| 真机验证 | QQ / Edge / 豆包 + 真实 LLM + 邮件 | 桌面 QQ 收发消息、Edge 驱动 DeepSeek/豆包、阿里云百炼 qwen3.5-plus function-calling 驱动多步文件系统工具调用（证据 `docs/evidence/agent_qwen_live.md`）；邮件 SMTP 发送 + IMAP 收件箱到达核验真机实跑（证据 `docs/evidence/email_live_qq.md`） |
| 百炼服务端工具 | 2 项 | 云端透明执行、无 tool_calls 往返：`enable_search` 联网搜索 + `enable_code_interpreter` 代码解释器（沙箱里写并运行 Python 解数学/数据分析，官方建议与搜索+思考模式同开）；纯计算回合自动启用、模型不支持自动停用（09-08 接入） |

> 注：`docs/test_report.md` 是第 2 周检查点快照（65 个），仅存档；当前以进度跟踪/README 的 512 为准。

---

## 3. 架构与分层（画图素材）

```
┌─────────────────────────────────────────────────┐
│ Agent（LLM）                                    │
│  核心:tool_registry.register(...)               │
├─────────────────────────────────────────────────┤
│ OSServiceAPI（MCP 风格接口）                    │
│  execute_tool / run_skill / list_available_*     │
│  返回 {success, tool/skill, result/error}        │
├─────────────────────────────────────────────────┤
│ ToolRegistry(57)   SkillLibrary(26)             │
│  安全分级safety + 确认门  分类classifier 执行executor │
├─────────────┬────────────────────┬──────────────┤
│ 文件/系统    │ app_controller.py  │ browser_controller.py │
│ (41 工具)    │ 键鼠模拟 Win/Linux  │ CDP 直连浏览器内核    │
└─────────────┴────────────────────┴──────────────┘
```

> 模型可调度面（Agent 编排层，2026-09-07 扩展）：主 LLM（OpenAI 兼容云端）→ **FailoverClient 自动降级**
> （主 API 失败自动切本地 Ollama → 确定性脑；支持运行时热切换 + `force_local_mode()` 纯本地）；
> 服务端联网搜索（百炼 enable_search）/ 本地搜索工具 / 纯对话模式（tool_free）由**联网总开关**统一管理。

要点：
- 接口参考 **MCP tools/list、tools/call** 设计，与第 3 组 ToolClient 对齐错误码（`TOOL_NOT_FOUND` / `TOOL_ERROR` / `SKILL_NOT_FOUND` / `SKILL_ERROR`）。
- 抽象层把**操作系统差异封在控制器里**：桌面键鼠有 Windows/Linux 双实现；浏览器走 CDP，浏览器屏蔽了 OS 差异，无需双实现。
- **安全分三层**：工具风险分级 + 高风险操作**确认门**（三档授权 `agent.authorization`：基础=全部高危需确认 / 高级=仅永久删除文件需确认 / 全自动=从不确认；Web 顶栏、CLI 可切换）+ Agent 编排层的执行引擎。

---

## 4. 两个创新点（报告的"亮点"）

### 4.1 真实软件自动化：应用操作（桌面 QQ）

**问题**：GUI 软件（QQ 自绘 UI）没有返回给外部程序的值、没有无障碍树，"点哪、进没进对会话"都不可见。

**方案（都是可复用的机制）**：
1. **剪贴板读回验证**：任何操作后用"截图 / 读剪贴板"拿证据，不靠猜。
2. **探针定位文本框**：对一段坐标带逐点 `点击 → 粘贴探针串 → Ctrl+A → Ctrl+C → 读剪贴板`，
   只有真点中文本框、复制真实发生时，剪贴板才含探针串 → 无假阳性定位搜索框/输入框。
   （细节：Ctrl+A 与 Ctrl+C 之间再清一次剪贴板，排除"粘贴源本身就把探针放进剪贴板"的假阳性；
   命中后 `Ctrl+A+Delete` 清场，避免探针污染后续输入。）
3. **OCR 标题门**：点搜索结果行后裁剪聊天标题横带交给视觉 OCR，标题含目标关键词才允许发送；
   "识别哪个会话是对的"留在 Agent 层，技能本身纯确定性。5 行不命中即报错、零发送。
4. **窗口矩形锚定坐标**：`get_window_rect` 取窗口位置后按比例换算各区域，分辨率/拖动位置变化不失效。

### 4.2 浏览器结构化控制（CDP）

- **快照索引**：`snapshot()` 给可交互元素打 `data-agentic-idx` 标记并返回压缩清单，
  Agent 按 index 操作，规避 CSS 选择器幻觉与大 DOM 问题。
- **中文安全输入**：走 CDP `Input.insertText` 直插文本，避免剪贴板/按键盲操作。
- **事件驱动等待**：`wait_for(selector/text)` 轮询替代固定 sleep，适配 SPA 异步渲染。

---

## 5. 真机验证记录（演示视频/截图素材）

| 用例 | 结果 | 一句话 |
|------|------|--------|
| QQ 全自动搜索并发消息到"我的手机" | ✅ | 探针定位搜索框 → OCR 门确认标题=目标 → 发送成功，截图取证 |
| QQ 读取会话并回复 | ✅ | 真实会话读内容 + 自动回复 |
| QQ 群“一中兄弟会”读后回 | ✅ | OCR 读真实群聊上下文 → 据聊天信息回一条不编造的短回复 → OCR 复验入群、输入框已清空 |
| Edge 浏览器给 DeepSeek 发"你好" | ✅ 一次性 | snapshot 定位输入框 → 输入 → 回车 → 读到回复"你好！很高兴见到你！😊"（需登录） |
| Edge 给豆包发"你好"（**游客模式，免登录**） | ✅ 可无人值守 | 全新独立档案=豆包眼里的新设备→游客额度重置；contenteditable 输入条真实鼠标点击聚焦 → insertText → 回车 → 读到多条回复（DOM+OCR 取证） |
| 阿里云百炼 qwen3.5-plus（真实 LLM · 多步文件系统对话） | ✅ 2026-09-06 | function-calling 驱动 Agent 完成"用户一句话 → 多步文件/系统工具调用 → 自然语言回复"闭环，证据见 `docs/evidence/agent_qwen_live.md` |

> 2026-09-08 补：**Web UI 产品体验轮迭代**（本轮 6 项全真机验证）——
> ① **历史会话自动标题**：会话标题自动取用户首条消息第一句（首个标点前、超 20 字截断），修复了原来 `title or "新对话"` 导致自动命名从不生效的 bug，旧会话加载时自动迁移补标题；浏览器实测旧会话全部变为可辨识摘要、新对话发消息后标题即时更新。
> ② **语音输入 🎤**（Web Speech API，零后端）：输入框左侧麦克风按钮，点击开始/再点关闭，**持续倾听**（停顿不终止）、识别结果**追加**不覆盖；实测交互与错误降级（无麦克风/服务不可用→红条提示）符合预期。
> ③ **思考内容折叠**：深度思考默认折叠成一行，点展开后限高 300px 可滚动看全部（对标 DeepSeek/Trae）。
> ④ **历史侧栏增强**：每次启动新开空对话、按最近访问排序并显示时间、点击可回看历史对话、超 20 条先加载最近 20 条。
> ⑤ **停止生成**：生成中发送按钮变红色「⏹ 停止」，秒级打断死循环，已输出部分保留落盘。
> ⑥ **本地模型乱码修复**：Ollama SSE 流按 UTF-8 增量解码（原 requests 按 ISO-8859-1 推断导致 `æ°è½` 式乱码），三个本地模型实测全正常。全量 494 单测通过。

> 2026-09-06：真实 LLM 链路补齐并真机跑通——接入阿里云百炼 qwen3.5-plus，以 function-calling 驱动 Agent
> 完成"用户一句话 → 多步文件系统工具调用 → 自然语言回复"闭环；本次补齐 system prompt、流式终止、
> 多工具 id 配对、JSON 兜底、base_url 归一等细节。真机对话证据见 `docs/evidence/agent_qwen_live.md`；
> 当前 415 个单元测试全部通过（2026-09-06 起陆续增：Agent 可调组合技能接线、截图理解/过程产物生命周期、统一视觉源 core.vision_bridge、邮件全链路 send_email、授权档位三档、QQ『先读后回』read_qq_chat + 联网白名单、HTTP 联网搜索 web_search 等用例）。

> 2026-09-06 补：给 Agent 接通"眼睛"——`config.supports_vision=true`，qwen3.5-plus 原生多模态
> （真机探测：内联一张真实 PNG 后模型回 OK）；修掉"把截图文件路径当 base64"的坏图 bug；
> 新增 Agent 可调能力函数 **screen_inspect**（截图理解：视觉模型看图 / 无视觉走项目内视觉桥
> core.vision_bridge，即 config.yaml 的 vision_bridge 段），
> 并在 SYSTEM_PROMPT 加"不确定/出错先自己截图看、不许直接说做不了"守则；截图等过程产物单独建
> 会话产物目录（同学建议的"产出分离+可被下一技能复用"），支持「清理截图/清理产物」与 `/reset` 联动清空。
> 规范与验证见 `docs/adr/0009-agent-screen-inspect-artifacts.md` 与 `docs/evidence/agent_qwen_live.md` §9。

> 2026-09-06 补：**可移植性收口（项目内视觉桥 + config.yaml.example）**——原先"看屏幕/OCR"默认依赖
> 这台机器 `~/.claude/qwen-vision` 的外部 node 脚本（跑 demo 前得先有它），已迁进项目内
> `core/vision_bridge.py`：`llm.supports_vision` 决定用谁看图/OCR——主对话有视觉(true)直接用
> 主模型读图（不必第二个 key）；主对话纯文本(false)走 config.yaml 顶层**独立 `vision_bridge` 段**
> （可与主对话不同 key/厂商，即"填两个 API"）。删除 demo/文档里 `C:\Users\34808\...` 本机绝对路径，
> 新增 `config.yaml.example` 模板：别人拿到项目，复制成 config.yaml、照注释选一种填法（单 key 有视觉
> 主模型 / 双 key + 视觉桥）即可全功能。外部 node 桥仅作显式 `vision_js`/`AGENT_VISION_JS` 的旧通道保留。
> 全量测试 **343 全绿**。

> 2026-09-06 补：**邮件全链路（自验证技能 send_email）**——用纯标准库 `smtplib/imaplib/email`
> 新增协议层 `core/email_client.py` + Agent 白名单组合技能 **send_email**：SMTP 发信后自动 IMAP 回读
> 发件箱『已发送』核验已落库；config.yaml 的 email 段配 `verify_inbox`（收件侧邮箱 IMAP）再核验收件人
> 收件箱『确实到达』（**双端闭环**），回读未命中如实报 found=False、绝不把没验到当成功。对外发送走
> HIGH 确认门。授权码与 llm.api_key 同级敏感：只进 config/env、入库留空。技能 24→25、工具 57 不变。
> 全量测试 343→**368 全绿**（新增 tests/test_email.py 25 例）。真机示例 `examples/email_demo.py`
> （直驱技能 / `--agent` 真实 LLM 双模式 + 收尾独立 IMAP 核验）。**可写进报告的"验证代替假设"**：
> 跟 QQ 发送带 OCR 核对门同理，邮件能力把"我发出去了"升级成"IMAP 回读证据链：已发送落库 +（可选）
> 收件人收件箱命中"——发送这类外发动作不靠一次 API 返回就宣称成功。
>
> 2026-09-06 **QQ 真机实跑 send_email**：主号 `3480870832@qq.com` → 小号 `3837227570@qq.com`
> 发"你好呀，小余人"——SMTP 接受、**IMAP 到达核验命中（收件箱真收到）**。实跑暴露并修复 3 个真实坑：
> ①QQ 投递改写 Message-ID（`<…@miniyu.local>` → `<tencent_…@qq.com>`）②中文主题存 RFC2047 编码
> （→ `_folder_search_ids` 头解码后按主题兜底）③发件箱名 `Sent Messages` 带空格，imaplib 不加引号致
> `EXAMINE BAD`（→ 按 RFC3501 加引号）。另发现 **QQ 授权码 SMTP 不在发件箱『已发送』留副本** → 发件箱
> 未命中 ≠ 没发，双端闭环下以收件箱到达核验为铁证（demo 结论逻辑已改）。单测 **368→370**（test_email
> 25→27，+2 条回归）。证据 `docs/evidence/email_live_qq.md`。
> 2026-09-06 补：**授权三档（基础 / 高级 / 全自动）**——把「高危操作确认门」从二值开关升级成可切换授权档位
> `agent.authorization`：base 全部高危需确认（现状）；advanced 仅**永久删除文件**需确认，其余 HIGH
> （run_command、杀进程、QQ 发消息、发邮件）自动放行；full 从不确认（含删除）。纯函数
> `safety.should_confirm(level,name,risk)` 决策 + Agent 运行时确认门改读档位（旧 `confirm_high_risk=false`
> ⇔ full，配置不回归）；引擎级 fail-safe（call_safely/run_skill_safely）保持按 HIGH 不变，QQ 发送的
> 屏幕 OCR 核对门在高级/全自动下仍生效（自动放行≠盲发）。Web 顶栏授权下拉 `/switch-authz`
> （持久化 config.yaml + 热改运行中 agent 不丢会话，切「全自动」先弹一次浏览器确认把关）+ CLI `/authz`。
> 测试 370→**391 全绿**（15 个测试文件，新增 `tests/test_authz.py` 21 例）。
>
> 2026-09-06 补：**QQ『先读后回』只读技能 read_qq_chat + 联网白名单（回应"助手不会变通"）**——真机让它
> "找 QQ 某群、看聊天回一句话"，它答"send 技能只能发、看不到聊天历史"。**根因不是模型笨，是能力没暴露**：
> ReAct 模型能"想"的范围只有上下文里出现的函数，QQ 当时只暴露了能发的 `app_send_message`、没有"读"，所以
> 读能力对它而言**不存在**。修复思路＝把"读/查"也做成模型天然可走的函数，而不是等它自己"变通"：
> - **read_qq_chat**（只读、不外发）：进目标会话 → OCR 核对会话标题（读错群会失败关闭）→ 截图裁聊天消息区
>   → `core.vision_bridge` 视觉桥转写最近聊天返回文本。skill 能"截图分析"但模型要先知道这个函数存在，
>   它才调得动——这正是"能读却没用上"的解法。
> - Agent 白名单 2→**5**（app_send_message / read_qq_chat / send_email / browser_search / browser_extract），
>   SYSTEM_PROMPT 显式教路径：QQ 找群看聊天＝先 `read_qq_chat` 再 `app_send_message`；联网查资料＝
>   `browser_search`（bing/baidu）→ `browser_extract`（读正文），标注来源、不编造。技能 25→26、工具 57 不变。
> **可写进报告的工程观点**：给 LLM 的"能力清单"＝函数暴露 + prompt 守则；它不会用你没给它的工具，所以
> 提升智能靠"把更多能力变成白名单函数"，而非要求模型发散。测试 391→**404 全绿**（16 个测试文件，
> 新增 `tests/test_agent_read_web.py` 11 例 + `test_agent_skills` +2）。

> **2026-09-06 联网搜索改 HTTP 直连（真机"思考 3 分钟仍超时"修复）**：真机让 miniyu 搜 CSGO 赛果
> → 反复"搜索超时"约 3 分钟。根因＝旧 `browser_search` 走无头浏览器"打字+合成回车"，**CDP 合成回车
> 在必应/百度都不触发提交**（词已打进框、URL 停在首页），内部 wait_for 每次必 10s 超时、模型重试数次
> 堆到分钟级；百度另有"安全验证"人机墙。修复＝新增 `core/web_search.py`（纯标准库、零依赖）**HTTP
> 直接请求必应结果页再解析 `b_algo`**，与云端联网搜索同构；基址直连 `cn.bing.com`（`www` 会 302 跳 cn、
> CN 网络下二次握手偶发被重置 WinError 10054）+ **3 次连接级重试**兜底，实测 **~0.6s** 返回 5 条。
> 可写进报告的工程点：**给 LLM 的联网"开关"必须稳到单次秒回**——一个不稳定/慢的函数会让模型反复重试，
> 把偶发延迟放大成"卡死几分钟"。测试 404→**415 全绿**（17 个测试文件，新增 `tests/test_web_http.py` 11 例，离网不联网）。

三个可直接跑的示例在 `examples/`：
- `qq_send_message_demo.py`：真实 QQ 全自动发消息（含 OCR 门）。
- `browser_deepseek_demo.py`：真实 Edge 给 DeepSeek 发消息（CDP，**需登录**）。
- `browser_doubao_demo.py`：**免登录**豆包游客对话，可无人值守复跑。

> 为什么浏览器 demo 能"免登录"？豆包网页版有游客模式；我们的调试浏览器每次用**全新独立档案**启动，
> 豆包把它当新设备、游客额度重置，发一句 + 读回复足够。DeepSeek 无游客模式、会话 cookie 不跨进程，
> 所以换成豆包做免登录演示目标。
> 附实测小坑（可写进报告）：contenteditable 输入条**必须真实鼠标点击聚焦**才能回车发送，
> 仅 JS `focus()` 后按 Enter 豆包会忽略——这体现"用浏览器级可信事件而不是伪造事件"的工程取舍。

> **2026-09-07：Agent 编排层"可靠性 + 可移植"收官（本地模型兜底 / 模型热切换 / 换模型不丢工具 / 联网总开关 / 流式思考展示）**
> 本周把 Agent 从"只依赖云端 API 演示"做成**断网也能演示、任意模型都能接**的形态，含 6 组真机验证：
>
> 1. **FailoverClient 自动降级（主 API → 本地 Ollama → 确定性脑）**：主 API 超时/鉴权失败/网络不通时自动切
>    本地 Ollama（纯 CPU 可跑的小模型），5 分钟定时重试、恢复自动升级回主 API；不配 fallback 段则行为不变。
>    可写进报告的工程点：**给 LLM 接"备用电源"**——演示现场断网也不至于整个 Agent 瘫掉，还能离线自证
>    "Agent 编排"本身不依赖任何一家云厂商。
> 2. **模型热切换三连修（Web UI，真机逐一复现）**：① 热切换失效——FailoverClient 模型名在 `primary.model`、
>    路由没查到就静默跳过；② 切到**不支持 function calling 的模型**对话报"Failed to fetch"——实为上游 HTTP 400
>    （历史残留 `tool_calls`/`tool` 消息被拒）→ 换模型前 `_sanitize_tool_messages()` 自动清理工具消息转普通文本；
>    ③ 本地模型"切而不换"——主 API 可用时永远走云端 → 加 `force_local_mode()` 显式本地。
>    模型下拉框重写为**可搜索 Combobox**（☁️在线 / 🖥️本地 分组，点选即切）。
> 3. **换模型后 Agent"看不见 57 工具"根因 + 文本协议自适应**：ReAct 只在 `provider=openai_compatible` 时才把
>    tools 发给 LLM；配置 fallback 后 provider 变 `failover`，**工具列表根本没发给模型**，磁盘/QQ 全失效。
>    修复一：failover 分支正常透传 tools；修复二：**对不支持 tools 参数的模型（如 deepseek-r1-distill 实测 400）
>    自动降级为"文本协议"**——工具 JSON Schema 注入 system prompt，模型用 `<tool_call>{...}</tool_call>` 标记
>    输出调用，客户端正则解析回 ReAct 循环，对标 Qwen-Agent/早期 LangChain 对无 function calling 模型的通用解。
>    可写进报告：**function calling 从"模型必须有"变成"运行时探测 + 双协议自适应"，任意模型都能接工具链**。
> 4. **接入百炼服务端联网搜索（enable_search）+ 联网总开关**：不开自研搜索引擎，直接用阿里云百炼云端工具
>     `enable_search: true`（真机 qwen3.6-flash 问"杭州天气"直接给实时数据）；与本地 62 个函数同请求实测无冲突，
>     生效期间自动隐藏本地 browser_search、browser_extract 保留作深读。顶层 `web_search.enabled` 一个开关管
>     所有联网能力（DeepSeek 同款交互）：关=完全离线、如实告知"无法获取"，不编造。
> 5. **本地模型纯对话模式（tool_free）+ Web SSE 流式 + DeepSeek 式思考展示**：实测本地 7B 纯 CPU 带 63 工具
>    schema 首 token 要 33~150s、写 800 token 要 132s → 本地模型只做聊天不背工具，实测首 token 降到 9~20s；
>    SSE 全链路改造：`/chat` 立即返回 task_id → 后台 `run_stream` 逐 chunk 进事件队列 → `/task/<id>/events`
>    推送（reasoning/token/tool_call/confirm/done/error + 15s 心跳），前端思考过程实时滚动 + 计时、完成折叠成
>    "已深度思考（用时 Xs）"，正文逐字渐进上屏。
> 6. **本地模型乱码修复 + 「停止生成」打断**：三个本地模型回复全变 `æ°è½æº` 式乱码——根因不是模型，是
>    Ollama SSE 响应头不带 charset、requests 按 ISO-8859-1 误解码 UTF-8 → 改**按字节读流 + UTF-8 增量解码**
>    （还天然容忍多字节跨网络块边界）；本地小模型死循环/超长输出时用户可**一键打断**（stop_check 三时机 +
>    `/task/<id>/stop` 端点 + 前端红色「⏹ 停止」按钮），保留已流出部分文本、终态 <0.5s。
>
> 全量测试 415→**494 全绿**（21 个测试文件；新增 test_tool_protocol / test_bailian_tools / test_tool_free_sse /
> test_stream_stop_encoding）。真机证据与复现脚本见进度跟踪 2026-09-07 六组与 `examples/` 下 `probe_*` / `e2e_*`。
>
> **2026-09-08：「人机交互体验」收官（思考折叠 / 历史会话 / 一键启动）**
> 1. **思考内容折叠（对标 DeepSeek / Trae）**：深度思考面板补全样式——回复完成后默认折叠成一行「已深度思考（用时 X 秒）▼」，点标题展开/再点收起；展开后内容框**限高 300px、框内可滚动**（再看长思考也不把整页撑高）。浏览器实测 300px 高 / scrollHeight 1680px 正常滚动往返。
> 2. **历史会话侧栏**：每次启动**新开一个空对话**，历史独立列出（一个 `.json` 文件＝一段对话）；按**最近访问**排序（切换即置顶 + 刷新时间）并在每项下显示「今天 HH:mm · N条」；点击任意会话在聊天区**回看该历史对话**；>20 条先显示最近 20、可点「加载更多」展开全部。
> 3. **`.bat` 一键启动乱码修复**：`run_miniyu_web.bat` / `run_miniyu_cli.bat` 原 UTF-8 中文在 cmd 旧代码页(GBK)下双击报 `'潰' 不是内部或外部命令`，改为**纯 ASCII 英文内容**后彻底免疫编码。
> 4. **交付即用**：`miniyu使用说明.md` 新增必读——首次使用/给别人部署前换**自己的 API key 与模型**；想用本地模型需**自行下载 + 配置**（给出 Ollama 安装 / `ollama pull` / `OLLAMA_MODELS` 存盘指引）。
>
> 浏览器实测功能 A（历史侧栏标题+时间显示、点击加载历史、访问置顶高亮）、功能 B（思考折叠+限高压顶可滚动）**全部通过**；全量测试仍 **494/494 通过**。

---

## 6. 诚实记录：一次"证伪"和一次 bug 修复（报告里显专业）

1. **Ctrl+F 键盘路线被真机证伪**：早期版本用 `Ctrl+F 搜索 → Enter`，记录为"验证成功"。
   真机复测发现桌面 QQ 会把焦点落在**资料卡**而非搜索结果，进不了会话——之前的"成功"是未充分核实的误报。
   于是重写为"点击 + 探针 + OCR 门"，ADR 0008 完整记录。
   → **结论句式**："用验证代替假设；对真实软件做端到端测试，而不是只测 Mock / 只看返回码。"
2. **探针串污染 bug**：探针确认命中后没清框内探针串，QQ 拿"探针+真关键词"整串搜索，
   一次"搜到"实为目标会话恰好打开被顶到顶部（碰运气）。已修复（命中即清场），217 测试全过 + 真机 OCR 复验搜索框干净。

---

## 7. 已知边界（真机自动化绕不过去的现实，报告里可作"局限与展望"）

1. Chrome/Edge ≥136 出于防 Cookie 窃取，**默认档案下忽略 `--remote-debugging-port`**；
   CDP 只能接带独立 `--user-data-dir` 的浏览器档案 → 不能直接驱动"用户日常默认浏览器"。
2. DeepSeek 会话 cookie **不跨浏览器进程重启**：同一档案重启即掉回登录页，自动化每次新进程都要重新登录。
3. 桌面版 QQ 自绘 UI、网页版 QQ 官方已关停 → 自动化必须以桌面版为真机目标，且依赖探针 + OCR 而非固定坐标/无障碍。
4. 一旦搜索结果行点错进错会话，下拉会关闭，同一轮无法再采样更下面的行 → `verify` 应优先命中靠前行（ADR 0008 局限）。

---

## 8. 复用/工程细节（加分项）

- **DPI 感知**：Windows `SetProcessDpiAwareness(2)` 让"截图坐标"与"点击坐标" 1:1，否则非 100% 缩放下错位。
- **64 位剪贴板**：ctypes 下 `GlobalAlloc/GlobalLock/GetClipboardData` 必须设 `restype=c_void_p`，
  否则句柄被截断成 32 位，报 `GlobalLock 失败`。
- **中文输入两套**：桌面键鼠走剪贴板 CF_UNICODETEXT + Ctrl+V；浏览器走 `Input.insertText`。
- **跨平台**：Windows/Linux 双控制器实现 + `safe_print` 防 GBK 终端崩溃 + 路径/临时目录自适应。

---

## 9. 下一步可写进"未来工作"

- 与第 3 组联调：以 Mock 59/26 对等跑契约测试。
- 更多 IM / 网页布局模板（`_layout_profile` 可扩展，目前内置 `qq_classic`）。
- ~~把"确认门"做成可视化确认 UI~~（已落地：Web 模态框 + CLI y/n）→ 升级为**三档授权**（base/advanced/full）可切换；展望："放行一次并记住该操作"、会话级授权等。
- **Agent 编排层（LLM 驱动 ReAct 闭环）**：打通"用户一句话 → Agent 自动调 OS 工具 → 自然语言回复"，
  是课程"Agentic OS"的题眼。设计稿已出：`docs/Agent编排层设计方案.md`（含离线确定性脑默认→可移植、
  换机跑测试+离线 demo 即证明）。真实 LLM 链路已于 2026-09-06 打通并真机验证（qwen3.5-plus function-calling，见 §5），可作为报告压轴演示。

---

## 10. 09-08 新功能四连（图片直读 / 编程项目 / 公式渲染 / 主题）

### 10.1 本地图片直读（read_image）——"能截屏≠能读文件"
- **用户反馈**：让它看 `D:\桌面\1.3.png` 里的题，它说读不了图片、只能靠截屏才识别到。
- **根因**：模型可见函数只有 `screen_inspect`（**截当前屏幕**再发给视觉模型），没有「**给文件路径 → 直接读文件**」的视觉通道。截屏走屏幕像素、读图走文件 I/O，两条路——模型此前只有前者。
- **解法**：新增 Agent 视觉能力函数 `read_image(path)`，双通道：
  - 主模型有原生视觉（`llm.supports_vision: true`，当前 qwen3.5-plus）→ base64 原图直接发给主模型；
  - 主模型无视觉 → 自动走 `core/vision_bridge`（独立第二视觉 API）把图转成文字描述；
  - 文件不存在 / 两视觉源都没配好 → **明确报错**，绝不瞎编。
- **数字**：模型可见函数全集 = **59 底层工具 + 5 白名单技能 + screen_inspect/read_image 视觉双通道 = 66**。

### 10.2 编程项目辅助（search_in_files + edit_file）——"真 AI 助手"补齐代码能力
- 新增两把工具补"查 bug / 改代码"缺口：
  - `search_in_files(root, keyword, include_exts, max_results, timeout)`：**按内容**搜关键词/正则（区别于按文件名的 `search_files`），跨文件、带行号与原文、可限扩展名、超时保护；
  - `edit_file(path, old_text, new_text, occurrence)`：找一段原文**精确替换**（默认第一处 / `all` 全换），保留原编码，返回编辑摘要——比"整文件重写"安全。
- **工作流守则（SYSTEM_PROMPT 第 11 条「先侦察、后动手」）**：list_directory → search_files（按文件名）→ search_in_files（按内容/报错信息）→ read_text_file（大文件分段）→ 理解后才 edit_file/write_text_file → run_command 实跑验证 → 中文总结改动与原因。
- 风险分级：search_in_files=READ_ONLY（免确认）、edit_file=MEDIUM（走确认门）。

### 10.3 Web 公式渲染（KaTeX + marked + DOMPurify）——"数学符号正常书写"
- **用户反馈**：指数/根号等显示成 `$A^2-3A-2E=O$` 和 `\frac` 原文。
- **根因**：模型输出 Markdown+LaTeX 混合文本，前端直接 `innerText` 原样输出，没做渲染层。
- **解法（行业标准，Open WebUI / Lobe-Chat 同款）**：marked 解析 Markdown → DOMPurify 消毒 → KaTeX auto-render 渲染 `$...$`/`$$...$$`。
- **关键坑（实测踩到）**：marked 会**吞 LaTeX 反斜杠**（`\frac` → `frac`），必须先**提取保护** `$...$` 片段为私有区占位符 → marked 转 HTML → 还原 → 再交给 KaTeX，否则块级公式渲染失败。
- **离线可用**：KaTeX/marked/DOMPurify 连同字体**本地打包** `examples/static/vendor/`（Flask `/vendor/<path>` 托管），CDN 仅作 onerror 兜底。
- 浏览器实测：行内 4 公式 + 块级 1 公式全部渲染成 `katex`/`katex-display`，无残留 `$`，Markdown 加粗/代码正常。

### 10.4 深浅主题（CSS 变量 + localStorage）
- 顶栏「会话: N」徽章左侧 ☀️/🌙 按钮（用户指定位置）→ `:root[data-theme]` 变量切换深/浅色；
- `localStorage('miniyu-theme')` 记忆 + `<head>` 首帧前应用（**防闪烁**）；
- 全部 UI 元素 `var(--*)` 化，无硬编码颜色残留；浏览器实测：切换、刷新保持均通过。

---

## 11. 09-08 新功能四连（二）：百炼代码解释器 / 前端锁定 / 工具折叠 / 性能调优

### 11.1 用"官方云端代码解释器"算数学——不自研沙箱
- **做什么**：纯计算/数据分析提问（「123的21次方是多少？」「算一下这组数的平均值」）自动启用**百炼服务端代码解释器**——模型在阿里云沙箱里写并运行 Python，返回**逐位精确**结果。实测 qwen3.7-flash / qwen3.5-plus 算出 123²¹ = 77,269,364,466,549,865,653,073,473,388,030,061,522,211,723 全对（此前模型只能硬算/乱报）。
- **关键决策与坑**（官方文档 + 真实 API 探针 `examples/probe_bailian_code_interpreter.py` 五场景实测）：
  - Chat Completions 模式的启用参数是**顶层 `enable_code_interpreter: true`**——`tools` 数组里的 `{"type":"code_interpreter"}` 是 **Responses API** 写法，本端点会 400 `'function' is a required property`；
  - **仅支持流式**（"Non-streaming mode does not support Code interpreter"）、**不能与本地 60+ 函数工具同请求**（"Agent mode does not support tools"）、官方建议配 `enable_thinking`；
  - **回合级智能分流**：命中计算关键词（`_PURE_CALC_RE`）且不涉及本地资源（`_LOCAL_REF_RE`）→ 该回合 `tools=None` 走云端解释器；涉及本地文件/系统 → 照常走本地工具。用户无感。
- **自适应降级**：模型不支持时（400 含 "code_interpreter" / "Agent mode..."）自动停注重试、换模型后重新探测——延续本项目"运行时自适应"主线，不影响正常对话。

### 11.2 前端"钉死"在窗口里 + 工具调用折叠面板（Web UI 体验）
- **布局锁定**：`html, body { overflow:hidden; overscroll-behavior:none }`，滚动只发生在聊天区内部——"整个界面被划出窗口"的滑动问题根治（横向纵向都锁死）。
- **工具调用折叠面板**：工具调用从"平铺一行行"改为**仿深度思考的折叠面板**——同一轮多次调用归入同一面板、默认一行「工具调用 (N 个) ▼」，点标题展开/再点收起，展开后**限高 260px、超出在框内滚动**。"过程可见性"（可展开审计模型干了什么）与"不占屏幕"兼得。

### 11.3 性能调优：向开源社区要的两板斧（省 token / 省延迟）
- **工具结果超长截断**（对标 LangGraph 社区 `TOOL_OUTPUT_MAX_TOKENS` 实践）：`run_command` 输出/文件内容等大结果默认截断到前 **4000 字符**并附"可针对性再读"说明（`agent.tool_result_max_chars` 可调，0=不截断）——大结果不再全量回传 LLM，省 token 且加速请求，不丢关键结论。
- **HTTP 连接池复用**（`requests.Session` + `threading.local` 线程隔离，Session 非线程安全）：每次请求省 TCP/TLS 握手约 **50-200ms**——Web 端后台多线程流式场景收益明显（多轮 ReAct × 多并发任务叠加可观）。

## 12. 跨平台可移植性与他人部署（09-09）

### 12.1 定位：任务书要 Linux，开发却在 Windows —— 能不能双平台？
任务书要求"在 **Linux 桌面环境**实现 Agentic OS"（Ubuntu 22.04 + GNOME + Python 3.10，见设计任务书），而本项目一直跑在 Windows 上。本次逐文件审查确认：**架构天然双平台、无需改动**——
- **路径**：全项目相对路径 + `pathlib`，自动适配 `/` 与 `\`；进程产物临时目录用 `tempfile.gettempdir()`（Win `%TEMP%` / Linux `/tmp`），会话 `conversations/` 相对根目录，`config.yaml` 无绝对路径。
- **编码**：全项目 UTF-8，`safe_print()` 兜住 Windows GBK 终端；LLM 流式按 UTF-8 增量解码（Ollama 中文不乱码）。
- **依赖**：`requirements.txt` 全跨平台库（Pillow / websocket-client / psutil / requests / pyyaml，主体纯标准库）。
- **平台自适应层（核心卖点）**：`app_controller.py` 用抽象基类 `BaseAppController` + 工厂 `get_controller()` 按 `platform.system()` 分派——`WindowsAppController`(ctypes/Win32) vs `LinuxAppController`(xdotool/wmctrl/xclip/gnome-screenshot)；`browser_controller.py` 走**跨平台 CDP**（`_find_chrome` 双平台路径探测），屏蔽操作系统差异。

### 12.2 本轮新增（让"别人也能用"成为现实）
- **`setup_linux.sh` 一键配置**：装系统依赖 `xdotool/wmctrl/xclip/gnome-screenshot` + `pip install -r requirements.txt` + 从 `config.yaml.example` 生成 `config.yaml` + **检测 Wayland/Xorg** 给指引。
- **Linux 启动脚本**：`run_miniyu_web.sh` / `run_miniyu_cli.sh`（等价 .bat），`.gitattributes` 锁 `.sh`=LF、`.bat`=CRLF。
- **Wayland 检测提示**：Ubuntu 默认 Wayland 会话下 xdotool/wmctrl（X11 工具）不可用，`LinuxAppController.__init__` 检测 `XDG_SESSION_TYPE=wayland` 时提示切「Ubuntu on Xorg」——桌面自动化需 Xorg，仅文件管理/Web 聊天不受影响。

### 12.3 可移植性 = 发出去对方配自己的 key 就能跑
- **敏感字段不随代码分发**：`config.yaml` 里的百炼 `api_key`×2、QQ 邮箱 `auth_code`×2 靠 `.gitignore` + git skip-worktree **不入库**；随代码分发的是**空 key 模板** `config.yaml.example`（开发/运维分离，防 key 泄漏）。
- **对方四步上手**（Windows/Linux 通用）：装依赖 → `cp config.yaml.example config.yaml` 填自己的 key / 模型 / 本地 Ollama →（Linux）`bash setup_linux.sh` → `python examples/miniyu_web.py`。个人 key 与本地模型都不随代码走。

### 12.4 一个易踩的"跨平台"细节：语音输入
Web 语音输入用**浏览器原生 Web Speech API**，只支持 **Chrome / Edge**；正式推荐 Edge（微软语音、国内可用），Chrome 走谷歌服务大陆常连不上。非 Chromium 浏览器自动隐藏 🎤 按钮。已把该要求明确写入使用说明与 README。

## 13. 第 4 组提交文档体系（对照设计书评分/验收补齐，09-09）

通读设计书后确认第 4 组功能**无缺失且超额**（统一接口 59 工具 + 26 技能 + 调用统计 + 工具签名 + MCP 参考 + 57 Mock + 512 测试全绿），缺的只是可提交文档。已落 docs/：
- **第4组调研报告.md**（20 分）：UFO²/Tools 定位 → MCP 协议深入 → 竞品对标（UFO/ANOLISA/OpenInterpreter/LangChain）→ OS 持久性关联 → 文献，≥3000 字；
- **第4组设计文档.md**（30 分）：架构分析 + 接口契约 `tool_result_json` + 模块设计（ToolRegistry/SkillLibrary/Mock）+ mermaid 流程图 + 可行性 + 风险评估；
- **第4组单元测试报告.md**：512 全绿 + 设计书功能→测试映射；
- **第4组联调与集成报告.md**（20 分）：真实 LLM qwen3.5-plus function-calling + 邮件 SMTP/IMAP 闭环 + 百炼服务端工具，设计书 6 演示场景核对（核心=场景 6 整理下载目录）；
- **第4组交付说明.md**：提交/打包清单、一键复现、验收自检表、待办（声明最终只发 `config.yaml.example` 不含个人 key）。

## 14. 第 5 组职责核对与补齐（整体设计完整性，09-09）

对照设计书「5. 第 5 组：系统协调 + 安全 + RAG」逐项核查**整体项目**（第 5 组职责其实由本项目统一实现）：
- **安全层（SecuritySandbox）**：✅ 早已完整——`core/safety.py` 四层（风险分级 read_only/low/medium/high + 授权档 base/advanced/full + 确认门 + 预览），比设计书示例更完备；
- **SystemCoordinator（模块编排）**：⚠️ 之前只有 agent.py/executor.py 隐式编排 → **新增 `core/coordinator.py` 独立类**补齐（register/orchestrate 按 1→5→2→3→4 数据流，审计+RAG 存储）；
- **RAGKnowledgeBase（向量检索）**：❌→✅ **新增**，轻量「词频向量+余弦相似度」检索执行轨迹（中文 bigram 分词，零外部依赖，可换 embedding 升级）；
- **审计日志**：❌→✅ **新增** `AuditLog`（JSON Lines 可落盘、线程安全、事后查询）；
- **降级 Mock**：❌→✅ **新增** `MockCoordinator`/`MockRAG`/`MockAudit`，基于第 4 组 57 个同构工具 Mock 跑端到端编排；
- **与 Agent 集成**：`agent.run/run_stream` 结束自动记审计 + 存 RAG 轨迹（`agent.coordinator.enabled` 可关），测试 27 项新增全绿。

**调研报告字数**：汉字 2453 → **3076 字达标**（补「与第 5 组协同调研」章节：工具层=审计/RAG 数据源、签名验证与隐私保护、降级 Mock 协同）。
**汇报 PPT**：`docs/第4组汇报PPT.pptx`（18 页 ≥15 页：封面/目录/UFO²定位/调研/架构/模块/安全RAG/Mock/测试/端到端演示/场景全覆盖/性能跨平台/成果/问题/分工/总结）。
