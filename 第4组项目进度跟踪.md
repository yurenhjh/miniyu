# 第4组（工具注册 + OS Skills → 桌面 AI 助手 miniyu）项目进度跟踪

> 最后更新：2026-09-11
> 记录人：第4组
> 组号：第4组 | 对应组件：UFO² Tools | 层次：工具服务层
> 
> **项目范围已扩展**：从最初的"工具注册 + OS Skills"成长为完整的**桌面 AI 助手 miniyu**，
> 覆盖工具服务层 → Agent 编排层 → 用户交互层（CLI + Web）全链路。

---

## 一、总体进度概览

| 周次 | 计划任务 | 状态 |
|------|----------|------|
| 第1周 | 调研MCP协议，定义接口，实现Mock | ✅ **已完成** |
| 第2周 | 实现工具注册表，实现文件操作工具 | ✅ **已完成** |
| 第3周 | 实现OS Skills，与第3组联调（转第4组自研：Agent 编排层 + 真实 LLM 真机验证） | ✅ **已完成**（09-06） |
| 第4周 | 完善工具签名，参与整体系统集成 | ✅ 已完成（工具描述Schema、MCP接口、系统管理工具、工具签名） |
| 第5周 | Agent 编排层实现（LLM客户端 + 离线脑 + ReAct循环 + CLI） | ✅ **已完成** |
| 扩展 | 桌面 AI 助手 miniyu 完整系统（品牌化 + Web 界面 + 记忆系统 + 流式输出） | ✅ **已完成** |

---

## 二、项目范围说明

> **当前项目已超出"第4组工具注册"的原始定义，成长为完整的桌面 AI 助手系统。**

### 原始范围（第4组任务书）
```
工具注册表 + OS Skills → 仅工具服务层
```

### 当前范围（完整系统）
```
用户交互层 ──┬── CLI 终端界面（examples/agent_cli.py）
             └── Web 桌面客户端（examples/miniyu_web.py）
    ↓
Agent 编排层 ──┬── Conversation（对话记忆、多会话管理、持久化）
               ├── Agent（ReAct 循环、安全确认门、流式输出）
               └── LLMClient ──┬── DeterministicBrain（离线脑）
                               └── OpenAICompatibleClient（真实 LLM）
    ↓
工具服务层 ──┬── ToolRegistry（61 个工具）
             ├── SkillLibrary（26 个技能，Agent 白名单 5：app_send_message/read_qq_chat/send_email/browser_search/browser_extract）
             ├── AppController（应用操作，Win32 + Linux）
             └── BrowserController（浏览器 CDP 控制）
    ↓
安全层 ──→ Safety（风险分级、确认门、ConfirmationDenied）
```

---

## 三、第1周任务完成情况

### 3.1 调研MCP协议
	- **状态：✅ 已完成**
	- `docs/第4组调研报告.md`（MCP 协议调研并入第 4 章）— MCP 协议调研
	- 覆盖内容：核心架构（Client-Server/JSON-RPC 2.0）、Tools规范（name/description/inputSchema）、Resources/Prompts/Sampling、传输层（stdio/Streamable HTTP）、安全模型
	- 包含对第4组接口设计的改进建议和与第3组的对接方案（详见 `docs/第4组接口文档.md`）

	### 3.2 定义接口
	- **状态：✅ 已完成**
	- `OSServiceAPI` 统一接口：`execute_tool()` / `run_skill()` / `list_available_tools()` / `list_available_skills()` / `get_tool_stats()`
	- 参考MCP协议设计，返回格式统一为 `{success, tool/skill, result/error}`
	- 参考MCP的tools/list→list_available_tools，tools/call→execute_tool

	### 3.3 实现Mock
	- **状态：✅ 已完成**
	- `MockToolRegistry` — 完整13个工具的Mock实现（原只有3个）
	- `MockSkillLibrary` — 完整4个技能的Mock实现（原缺search_file）
	- `MockOSServiceAPI` — 统一Mock入口
	- 所有Mock返回均带 `[MOCK]` 标记，便于区分

	### 3.4 检查点1提交材料
| 材料 | 状态 | 说明 |
|------|------|------|
| 接口文档 | ✅ | `docs/第4组接口文档.md` |
| Mock代码 | ✅ | `mock/` 模块，13工具+4技能全量Mock |
| 调研报告初稿 | ✅ | `docs/第4组调研报告.md` — MCP协议调研并入第 4 章 |

---

## 三、第2周任务完成情况

### 3.1 实现工具注册表（ToolRegistry）
- **状态：✅ 已完成**
- 核心功能：`register`, `unregister`, `list_tools`, `get_logs`, `call`
- 新增功能：**`get_stats()`** 调用统计（总次数、各工具次数、成功率、Top5排名）
- 新增功能：**`clear_logs()`** 清空日志
- 异常处理完善，参数校验

### 3.2 实现文件操作工具（31个）
| 工具名称 | 状态 | 备注 |
|----------|------|------|
| copy_file | ✅ | 使用shutil.copy2 |
| move_path | ✅ | 使用shutil.move（文件/目录，旧名 move_file 为别名） |
| delete_file | ✅ | 检查文件存在 |
| rename_path | ✅ | 使用Path.rename（文件/目录，旧名 rename_file 为别名） |
| create_directory | ✅ | 支持parents递归创建（旧名 create_folder 为别名） |
| delete_directory | ✅ | 使用shutil.rmtree（旧名 delete_folder 为别名） |
| list_directory | ✅ | 返回name/type结构 |
| sort_directory | ✅ | 排序目录（name/size/mtime，升降序，带元信息） |
| index_files | ✅ | 递归建立文件索引，支持 persist/load JSON |
| manage_archive | ✅ | 打包/解压 zip（action=compress/extract） |
| file_exists | ✅ | 使用Path.exists |
| get_file_size | ✅ | 使用Path.stat |
| read_text_file | ✅ | UTF-8编码 |
| write_text_file | ✅ | UTF-8编码 |
| current_directory | ✅ | 使用Path.cwd |
| run_command | ✅ | 返回stdout/stderr/returncode |

### 3.3 实现OS Skills（4个）
| Skill | 状态 | 备注 |
|-------|------|------|
| organize_downloads | ✅ | 按文档/图片/压缩包分类 |
| system_info | ✅ | 系统/CPU/Python信息 |
| cleanup_temp | ✅ | Windows TEMP清理 |
| search_file | ✅ | 递归文件搜索（substring/exact/wildcard/regex） |

### 3.4 检查点2提交材料
| 材料 | 状态 | 说明 |
|------|------|------|
| 模块代码 | ✅ | `core/` 下4个模块（含utils.py） |
| 设计文档 | ✅ | `docs/第4组设计文档.md` |
| 单元测试报告 | ✅ | `docs/第4组单元测试报告.md` — 当前 583 测试全通过（检查点 2 时 65 个里程碑） |

---

## 四、创新点：软件控制能力（应用操作 + 浏览器结构化控制）

> 在原“文件管理”基础上新增**软件控制能力**，使系统不仅能管理文件，还能像人一样操作软件（QQ、浏览器等）。这是本项目区别于纯文件管理方案的主要创新点。软件控制走两条技术路线：
> - **应用操作**（键鼠模拟）：控制 Windows/Linux 桌面窗口（QQ 等桌面应用）
> - **浏览器结构化控制**（CDP 协议）：直连浏览器内核，按语义精确操作网页元素，并返回结构化结果
> 工具合计：文件/磁盘/文本/批量等文件类 33 + 系统与管理类 10 + 应用操作 7 + 浏览器控制 9 = **共 59 个工具**；技能共 **26 个**。

### 4.1 抽象层 core/app_controller.py
- `BaseAppController` 抽象基类，统一 10 个原子能力：
  `list_windows` / `find_window` / `activate_window` / `send_hotkey` / `send_text` /
  `take_screenshot` / `click_at` / `read_clipboard` / `get_window_rect` / `set_clipboard`
  （后 4 个为"探针定位文本框 + 剪贴板读回验证 + 窗口矩形锚点"服务，2026-09-05 新增）
- 双平台实现：`WindowsAppController`（ctypes 调 Win32 API，DPI 感知 + 剪贴板 CF_UNICODETEXT）、`LinuxAppController`（subprocess 调 xdotool/wmctrl/xclip）
- 工厂 `get_controller()` 依据 `platform.system()` 自动选择实现
- 三大难点与对策：
  - 窗口激活失败（前台锁）→ `AttachThreadInput` + Alt 键技巧绕过
  - 中文输入乱码 → 剪贴板（CF_UNICODETEXT）+ Ctrl+V 粘贴
  - GUI 无返回值 → 截图 + 剪贴板读回验证操作结果

### 4.2 应用操作工具（7 个：原 6 + `click_at`）
| 工具名称 | 状态 | 备注 |
|----------|------|------|
| list_windows | ✅ | 枚举桌面窗口 |
| find_window | ✅ | 按标题/进程名匹配窗口 |
| activate_window | ✅ | 激活（置前）目标窗口 |
| send_hotkey | ✅ | 发送组合键（如 ctrl+f） |
| send_text | ✅ | 输入文本（剪贴板粘贴，兼容中文） |
| take_screenshot | ✅ | 桌面截图（用于结果验证） |
| click_at | ✅ | 在屏幕坐标左键单击（配合截图/视觉定位，探针定位文本框的基础） |

### 4.3 应用操作技能（+2，共 22）
| Skill | 状态 | 备注 |
|-------|------|------|
| app_open | ✅ | 打开指定应用 |
| app_send_message | ✅ | 应用内搜索目标并发送消息（点击+探针+可选 OCR 门；风险 HIGH，走确认门）。真机验证见 4.8 / ADR 0008 |

### 4.4 端到端验证（诚实记录，含一次证伪）
- ⚠️ 早先记录的“Ctrl+F 搜索群名并发消息成功”**实为误报**：真机复测发现桌面版 QQ（自绘 UI）的
  Ctrl+F / Enter 焦点会落在联系人**资料卡**而非搜索结果，进不了目标会话。
- ✅ 改走 **点击 + 探针定位 + OCR 标题门** 路线（ADR 0008），并在真实 QQ 对安全目标“我的手机”
  全链路真机验证通过：探针命中搜索框 → OCR 门确认聊天标题=目标 → 定位输入框 → 发送成功并截图取证。
- ✅ 真实 Edge 浏览器给 DeepSeek 发“你好”并读到回复“你好！很高兴见到你！😊”（CDP，一次性通过）。

### 4.5 浏览器结构化控制：core/browser_controller.py
- 基于 **CDP（Chrome DevTools Protocol）** 直连 Chrome/Edge 内核，浏览器屏蔽操作系统差异，无需 Windows/Linux 双实现
- 核心能力：`launch` / `attach` / `connect` / `navigate` / `snapshot` / `find` / `click` / `type_text` / `read_text` / `screenshot` / `wait_for` / `annotated_screenshot`
- 关键设计（参考开源 browser-use / agent-browser / LiteWebAgent / cdp-browser-mcp）：
  - **快照索引**：`snapshot()` 给可交互元素打 `data-agentic-idx` 标记并返回压缩元素清单，LLM 按 index 操作，规避 CSS 选择器幻觉与“DOM 过大”问题
  - **稳定 ref**（09-11）：`snapshot` / `find` 返回稳定 ref（元素 id / 结构路径签名生成），`click(target=ref)` 直接落点，DPR 后 DOM 重排不漂移
  - **SoM 视觉定位**（09-11）：`annotated_screenshot` 给页面截"交互元素带编号覆盖层"图，`click(target='som:N')` 真实鼠标点击；单一 InspectSession + URL/DOM 指纹失效判定
  - **中文输入**：走 CDP `Input.insertText` 直插焦点元素文本，避免剪贴板/按键盲操作
  - **事件驱动等待**：`wait_for(selector/text)` 轮询替代固定 sleep，适配 SPA 异步渲染
  - **惰性初始化**：控制器仅在首次调用浏览器工具时才实例化，节省资源

### 4.6 浏览器结构化工具（11 个）
| 工具名称 | 状态 | 备注 |
|----------|------|------|
| browser_launch | ✅ | 启动带调试端口的浏览器并连接 |
| browser_close | ✅ | 关闭 CDP 连接 |
| browser_navigate | ✅ | 页面导航（Page.navigate） |
| browser_snapshot | ✅ | 提取可交互元素索引清单（含稳定 ref，Agent 的“眼睛”） |
| browser_click | ✅ | 点击元素（统一 target=ref / som:N / CSS 选择器） |
| browser_type | ✅ | 向元素输入文本（Input.insertText，中文安全，支持 target=ref / som:N） |
| browser_read_text | ✅ | 读取页面/元素文本 |
| browser_screenshot | ✅ | 页面截图（Page.captureScreenshot） |
| browser_wait | ✅ | 等待元素/文本出现 |
| browser_inspect | ✅ | 截"交互元素带编号覆盖层"图（SoM 视觉定位，09-11 新增） |
| browser_find | ✅ | 按目标文字/名称/角色局部搜索元素并返回稳定 ref（09-11 新增） |

### 4.7 浏览器组合技能（+2，共 24）
| Skill | 状态 | 备注 |
|-------|------|------|
| browser_search | ✅ | HTTP 直连必应结果页→解析前几条{标题/链接/摘要}（只读、不开浏览器） |
| browser_extract | ✅ | 打开网页并按需提取正文内容（结构化阅读） |

### 4.8 真机验证记录与已知边界（真实软件自动化，非 Mock）

| 用例 | 结果 | 说明 |
|------|------|------|
| QQ 全自动搜索并发消息（目标：我的手机） | ✅ | 点击+探针定位搜索框 → OCR 标题门确认命中 → 定位输入框 → 发送成功并截图取证；事后 OCR 复验搜索框仅含“我的手机” |
| QQ 读取会话并回复 | ✅ | 真实会话内读取内容并用 `app_send_message` 回复 |
| Edge 给 DeepSeek 发“你好”并读到回复（CDP） | ✅ 一次性 | attach 专用档案 → snapshot 索引定位输入框 → insertText 输入 → 回车 → 读到“你好！很高兴见到你！😊”；该档案重启即掉登录（见边界 2），故改免登录目标 |
| Edge 给豆包（游客/免登录）发“你好”并读回复 | ✅ 可无人值守 | 全新独立档案=豆包眼里的新设备→游客额度重置；定位 contenteditable 输入条→**真实鼠标点击聚焦**→insertText→回车→读到“你好呀～…”“你好！有什么需要帮忙的吗？”等多条回复（DOM+OCR 取证） |
| QQ 群“一中兄弟会”：读取近期聊天并据实回一条 | ✅ | 视觉 OCR 读真实群聊上下文（绿龙/猫娘发言/柔韧性等闲聊）→ 依内容回一条不编造、不代他人表态的短回复（OCR 标题门确认目标群）→ 事后 OCR 复验消息已入群、底部输入框已清空 |

**过程中发现并修复的问题（可写报告）**
- 探针串污染 bug：探针确认搜索框命中后未清除框内 `G4probe…`，随后拼接真实关键词让 QQ 拿整串搜索，
  “搜到”其实是目标会话恰好打开被顶到顶部（碰运气而非真命中）。已修复（命中即 Ctrl+A+Delete 清场），
  修复后 217 测试全过 + 真机 OCR 复验搜索框干净。

**已知边界（写报告可引用）**
1. Chrome/Edge ≥136 出于防 Cookie 窃取，**默认档案下忽略 `--remote-debugging-port`**；CDP 只能接带独立
   `--user-data-dir` 的档案 → “直接驱动用户日常默认 Edge”不可行。
2. DeepSeek 会话 cookie **不跨浏览器进程重启**：同一档案重启即掉回 `/sign_in`，任何自动化新进程都要重新登录。
3. 桌面版 QQ 为自绘 UI：无无障碍树、Ctrl+F/Enter 落资料卡 → 用“探针（点击+粘贴+读回剪贴板）”与 OCR 门定位，
   不依赖固定坐标与视觉做判断。
4. 网页版 QQ 官方已关停，真机目标即桌面版 QQ。
5. 三个可运行示例已沉淀到 `examples/`：`qq_send_message_demo.py`（含 OCR 门）、`browser_deepseek_demo.py`（CDP 需登录）、`browser_doubao_demo.py`（CDP 免登录游客）。

### 4.9 邮件组合技能 send_email（+1，共 25）

邮件全链路做成 1 个**自验证技能**（镜像 app_send_message 形态，工具 57 不变、技能 24→25），
纯 stdlib（smtplib/imaplib/email），**零新增依赖**：

- `core/email_client.py`（协议层）：`smtp_send`（组信 + SSL/STARTTLS + login + send_message，自动生成唯一
  Message-ID 头）；`imap_verify_sent`（回读发件箱『已发送』核验已落库）；`imap_verify_arrival`（可选**双端闭环**：
  用收件侧邮箱的 IMAP 轮询其收件箱，核验『确实到达』）。连接拆 `_connect_smtp/_connect_imap` 等内部缝隙，
  单测 patch 即可、不真联网；`MailError` 中文指引 **fail-closed**：email 段没配好（缺 username/auth_code）在
  『发之前』就报错，绝不假装已发送。
- 接线：`_AGENT_SKILL_SCHEMAS` 白名单 + skill 方法（发→验编排）+ safety SKILL_META HIGH（对外不可撤回走
  确认门）+ SYSTEM_PROMPT 守则 8 + mock_skills 对等 + `config.yaml(.example)`/`agent_config`(ENV_MAP+DEFAULTS)
  顶层 email 段。授权码与 `llm.api_key` 同级敏感：只进 config/env、入库留空。
- QQ 细节（真机实测 2026-09-06）：IMAP『已发送』文件夹实为带空格的 `Sent Messages`（imaplib 不加引号直接
  EXAMINE 会 BAD → 按 RFC3501 加引号）；**QQ 授权码 SMTP 发信不在发件箱留副本** → 发件箱回读未命中≠没发；
  投递改写 Message-ID（→ `<tencent_…@qq.com>`）、中文主题存为 RFC2047 编码 → `_folder_search_ids` 解码头后
  按主题兜底匹配；双端闭环下以收件箱到达核验为铁证（163/Gmail 等留副本仍可查『已发送』）。
- 示例：`examples/email_demo.py`（直驱技能 / `--agent` 真实 LLM 双模式 + 收尾独立 IMAP 核验）。
- 测试：新增 `tests/test_email.py` 25 例（fail-closed / 协议层 SMTP+IMAP / 技能编排如实上报 / Agent HIGH 确认门 /
  Mock 对等），全量 **343→368 全绿**；QQ 真机实跑补 2 条回归（SELECT 加引号 / RFC2047 主题命中）→ **370 全绿**
  （test_email 25→27）。真机结果见 `docs/evidence/email_live_qq.md`。

### 4.10 授权档位（base / advanced / full，2026-09-06）

把「高危操作确认门」从二值开关升级成**三档可切换授权** `agent.authorization`：
- **base 基础授权**（默认，=现状）：所有 HIGH 操作（删除文件/目录、按类型批量删、清空回收站、执行命令、杀进程、QQ 发消息、发邮件）执行前都弹确认；
- **advanced 高级授权**：只对**永久删除文件**类确认（`delete_file`/`delete_directory`/`cleanup_by_type`/`empty_trash`），
  `run_command`/`kill_process`/`terminate_process`/`app_send_message`/`send_email` 自动放行；
- **full 全自动**：从不确认（含删除）。

实现：`core/safety.py` 加纯函数 `should_confirm(level,name,risk)` + `resolve_authz_level`（旧 `confirm_high_risk=false` ⇔ full，
配置不回归）；Agent 运行时确认门 `_execute_tool`/`_execute_skill` 改读 `self.authz_level`；`agent_config` ENV_MAP 加 `AGENT_AUTHORIZATION`，
新增 `set_config_authorization` 文本级写回（镜像 `set_config_model`）；`config.yaml(.example)` 加 `authorization: base` 注释。
交互：Web 顶栏新增**授权档位下拉** → `/switch-authz`（持久化 config + 热改运行中 agent 不丢会话；切「全自动」先弹一次浏览器 confirm 把关）；
CLI 加 `/authz [base|advanced|full]`。引擎级 fail-safe（`call_safely`/`run_skill_safely`）保持按 HIGH 不变；
QQ 发送的屏幕 OCR 核对门在高级/全自动下仍生效（**自动放行 ≠ 盲发**）。
测试：新增 `tests/test_authz.py` 21 例 → **370→391 全绿**（15 个测试文件）。

### 4.11 QQ『先读后回』read_qq_chat + 联网白名单（技能 26 / Agent 白名单 5，2026-09-06）

真机反馈"让它找 QQ 某群、看聊天回一句话，它说 send 技能只能发、看不到聊天历史"——**根因**：模型能"想"的
范围只有上下文里出现的函数；QQ 之前只暴露了能发消息的 `app_send_message`，没有"读"，能力没暴露＝对它不存在，
不是它笨。修复 = 把"读"和"查"也做成模型天然可走的函数：
- 新**只读**技能 **read_qq_chat**（只读不外发）：搜索进入目标会话 → OCR 核对会话标题（`make_ocr_verify`，
  目标不在结果前几行会失败关闭，绝不读到别的会话）→ 截图裁聊天消息区 → 交 `core.vision_bridge` 视觉桥转写
  最近聊天（缺视觉源明确报错、不假装读到），返回 transcript/会话名/截图；
- Agent 白名单 2→**5**：app_send_message / **read_qq_chat** / send_email / **browser_search** /
  **browser_extract**（原已有 browser 组合技能本节 4.7 即列，现提升为模型可直接调用的白名单函数）。
  SYSTEM_PROMPT 新守则：QQ 找群看聊天再回＝先 `read_qq_chat` 再 `app_send_message`（读→发两步，verify_ocr
  默认强制）；联网查资料＝`browser_search`（bing/baidu）→ `browser_extract`（打开网页读正文），须标注来源、
  不得编造；
- 技能 25→**26**（工具仍 57）；`core/safety.py` SKILL_META 记 read_qq_chat 为 READ_ONLY（任何授权档位都不进
  确认门）；mock 版技能库与白名单对等。
测试：新增 `tests/test_agent_read_web.py` 11 例（暴露 / READ_ONLY 无确认门 / Agent 强制 verify_ocr / 读多行
重试 / 无行命中失败关闭 / 无视觉源失败关闭 / 联网守则与 prompt / mock 对等）+ `test_agent_skills` +2（read_qq_chat
与联网技能 schema 只读校验）→ **391→404 全绿**（16 个测试文件）。

### 4.12 浏览器视觉定位增强（DOM-SoM-坐标混合架构，2026-09-11）

**目标**：把浏览器从"拿整页索引清单"升级为"结构化优先、视觉辅助、坐标兜底"的混合定位，
收敛到统一 `browser_click(target=…)`（ref / `som:N` / CSS 选择器三种 target 任选）。
设计依据 `docs/miniyu 浏览器与桌面视觉定位增强方案（DOM-SoM-坐标混合架构）.md` 与
`docs/miniyu Agent 浏览器操作规则 System Prompt.md`。

- **P0 统一数据模型**：新增 `core/uitarget.py` —— `UITarget`（source / ref / frame_id 预留 / visual_num /
  role / name / tag / bbox_css / center_css），坐标一律 **CSS 视口像素**，截图标注与真实点击按 DPR 换算设备像素
  （消除"记录 device 像素 + 双重缩放"历史坑）。
- **P1 截图分流修复**：浏览器动作后优先回传**页面截图**（而非桌面全屏），桌面操作才回桌面截图。
- **P2 ref 化**：`browser_snapshot` 返回稳定 ref（底层复用 CDP/元素 id + 结构路径签名，DOM 重排不漂移）；
  `browser_click/browser_type` 统一 `target` 兼容 ref / `som:N` / CSS 选择器，并对旧 index/selector/x,y 全兼容。
- **P3 browser_inspect（SoM）**：`annotated_screenshot` 给当前页面截"交互元素带编号覆盖层"图（PIL 画红框
  +编号，button/input 优先级排序、max_elements 不粗暴截断）；**单一 InspectSession**（num→ref 映射 + URL/DOM
  指纹 `_dom_sig`）做失效判定——页面导航/大变后旧编号 `_som_lookup` 返回空、`browser_click('som:N')` 抛
  `SoMStaleError` 明确报错，逼 Agent 重新 inspect（杜绝"用过期编号硬点"）。
- **P4 Agent 混合决策**：`_GUI_TOOLS` 收录 browser_inspect/find；SYSTEM_PROMPT 第 12 条改为
  「先 ref、再 find、再 snapshot、视觉需要才 SoM、最后才坐标」，并教"编号只是视觉标签、页面变化要重新 inspect"、
  "browser_click/type 返回的 url_changed/page_changed 是客观校验"。
- **P5 客观 Action Verification**：动作返回 `url_changed / page_changed`（URL + DOM 指纹对比）；**不做** AI
  语义判断"结果对不对"，判断交给 Agent 本身（避免引入不可控逻辑）。
- **browser_find（对 System Prompt 规则文档的闭环补齐）**：按目标文字 / 名称 / 角色 / 标签局部搜索并返回带
  稳定 ref 清单 + `in_viewport`；role 过滤做语义别名（`textbox/searchbox` 匹配 input/textarea、`button`/`link`/
  `checkbox`/`radio` 匹配对应标签）——大页面"先 find 缩小范围再按 ref 操作"，不必请求整页结构。
- **工具 59→61、浏览器 9→11**（新增 browser_inspect / browser_find）；**测试 558→583 全绿**：新增
  `tests/test_uitarget.py`（ref/som/坐标/构造与摘要）、`tests/test_browser_som.py`（优先级排序 / 带编号截图 /
  som 点击 num→ref→真实鼠标 / 会话失效抛错 / find 返回 ref 可点击）；更新 test_app_tools / 数量断言。
- **真实 Chrome 端到端自证**（`examples/browser_som_dom_demo.py`，无 LLM、走 ToolRegistry 工具面）：
  必应 `browser_find(role=textbox)` 定位 `input#sb_form_q`（ref=eid:sb_form_q）→ `browser_type` 中文"湖南大学"
  → 结果页 → `browser_find(搜索)` 按 ref 点击 → `browser_inspect` 带编号截图（dpr 1.75，编号/名字/中心全对齐）
  → `som:1` 真实鼠标点击成功；点击后 `page_changed:true` 自动使 SoM 会话失效（符合设计，逼重新 inspect）。

---

## 五、测试覆盖情况

| 测试文件 | 测试数 | 说明 |
|----------|--------|------|
| test_tool_registry.py | 92个 | 注册管理 + 57工具 + 异常 + 别名 + 错误码 |
| test_skills.py | 43个 | 基础4 + 8扩展 + 搜索模式 + Agent |
| test_os_service_api.py | 11个 | 统一接口调用链、统计 |
| test_mock.py | 33个 | Mock全模块验证 + 扩展技能冒烟 + 浏览器Mock冒烟 |
| test_app_tools.py | 19个 | 应用操作（含 click_at）+ 浏览器结构化工具/技能契约 |
| test_agentic.py | 19个 | 安全分级 / 确认门 / 执行引擎 / 任务分类（Agent 编排层） |
| **test_agent_config.py** | **6个** | 配置管理：加载/环境变量覆盖/类型转换/合并 |
| **test_conversation.py** | **28个** | 对话记忆：用户/助手/工具消息/滑动窗口/多模态/序列化/摘要/多会话管理器 |
| **test_llm_client.py** | **39个** | LLM客户端：离线脑匹配/拒绝/回退/工厂/响应格式/JSON兜底 + FailoverClient 自动降级（主/备用/兜底/流式/工厂） |
| **test_agent.py** | **16个** | Agent编排层：空输入/意图执行/风险确认/最大步数/多工具 tool_call_id 配对 |
| **test_model_switch.py** | **12个** | 模型切换：config.yaml 写回只改 model 行 / 非对话模型过滤 / 缺凭据不发网络 |
| **test_agent_skills.py** | **17个** | Agent 白名单组合技能：技能暴露成模型可调函数 / _execute_one 技能分发 + HIGH 拒绝 / 发送默认 OCR 门（含主 llm 有视觉也能 OCR）/ read_qq_chat·联网 schema 只读校验 |
| **test_agent_vision.py** | **19个** | Agent 截图理解：坏图修复 / 观测消息 / screen_inspect 双通道 / 产物目录与清理 / /reset 联动 / 工具集=60 / 统一视觉源 core.vision_bridge 选源 |
| **test_email.py** | **27个** | 邮件全链路 send_email：fail-closed / 协议层 SMTP·IMAP（Message-ID·已发送文件夹·头部命中，含 SELECT 加引号·RFC2047 主题解码回归）/ 技能编排如实上报（发→回读→到达）/ Agent HIGH 确认门 / Mock 对等 |
| **test_authz.py** | **21个** | 授权档位：should_confirm 表驱动 / resolve_authz_level 兼容（confirm_high_risk=false ⇔ full）/ Agent 确认门 base·advanced·full / set_config_authorization 写回+ENV 覆盖 |
| **test_agent_read_web.py** | **11个** | QQ『先读后回』只读技能 read_qq_chat + 联网白名单：暴露/READ_ONLY 不进确认门/强制 verify_ocr/读多行重试/无行命中·无视觉源失败关闭/联网守则/mock 对等 |
| **test_web_http.py** | **11个** | HTTP 联网搜索 core/web_search：b_algo 解析清洗/相对链接过滤/top 截断/摘要截断/bing_url/异常包装/瞬断重试（离网，不联网） |
| **test_tool_protocol.py** | **19个** | 工具协议自适应：文本协议解析/400 双分支自适应/流式适配/Failover 透传/切换重置 |
| **test_bailian_tools.py** | **19个** | 联网三态总开关：工厂透传/端点识别/注入与 400 停注/reset 还原/agent 三分支隐藏与提示/运行时切换联动 |
| **test_tool_free_sse.py** | **21个** | 本地模型纯对话：tool_free 忽略工具/reasoning 双字段捕获/Agent chat 分支/Failover 自动激活/SSE 队列管线（心跳·清理·确认） |
| **test_stream_stop_encoding.py** | **11个** | 流式 UTF-8 解码修复（Latin-1 推断回归锁/多字节跨块）+ 停止生成（run_stream stop_check 三时机/部分文本保留落盘/stop 端点/打断及时性） |
| **总计** | **494个** | **全部通过 ✅** |

---

## 六、代码质量

### 已完成改善
- ✅ 清理 `skill_library.py` 过多空行，统一PEP8风格
- ✅ `os_service_api.py` 统一走SkillLibrary.call()，避免getattr绕过
- ✅ 移除空目录 `tools/`、`skills/`
- ✅ 添加 `requirements.txt`
- ✅ README与实际文件结构同步

### 完成改善
- ✅ 新增跨平台兼容：`core/utils.py`（safe_print / 系统检测 / 默认路径）
- ✅ 所有中文输出通过 safe_print 保护，Windows GBK终端不再崩溃
- ✅ 默认下载/临时路径自动适配 Windows/Linux
- ✅ README 增加14. 跨平台兼容说明

### 待改善
- 设计文档（docx）内容未审阅（二进制格式无法读取）

---

## 七、功能要求完整对照

| 功能要求 | 状态 | 备注 |
|----------|------|------|
| ToolRegistry：统一管理所有工具 | ✅ | register/unregister/list_tools/call |
| SkillLibrary：封装系统管理操作 | ✅ | 24个技能，支持动态注册 |
| 工具描述 Schema（新增） | ✅ 已完成 | 2026-09-06 新增：ToolSpec类 + 自动从函数签名生成JSON Schema |
| 工具签名（可选） | ✅ 已完成 | 2026-09-06 实现：SHA256函数签名 + 完整性校验 |
| 系统管理工具（新增） | ✅ 已完成 | 2026-09-06 新增：进程管理/网络/环境变量共8个工具 |
| 安全Metadata同步 | ✅ 已完成 | 2026-09-06 新增工具已同步到safety.py |
| 调用统计 | ✅ | get_stats() + clear_logs() |
| 参考MCP协议设计接口 | ✅ | 见调研报告及接口设计 |

---

## 八、文件清单（当前）

```
group4_tools_os_skills/
├── core/
	│   ├── __init__.py
	│   ├── tool_registry.py         # 工具注册表（含统计 + 系统管理工具）
	│   ├── tool_schema.py           # 工具描述 Schema（ToolSpec，多格式转换）
	│   ├── skill_library.py         # OS技能库（代码风格已整理）
	│   ├── os_service_api.py        # 统一接口（已完善）
	│   ├── app_controller.py         # 应用操作抽象层（10 原子能力，含 click_at/read_clipboard/get_window_rect）
	│   ├── browser_controller.py     # 浏览器结构化控制（CDP）
	│   ├── safety.py                 # 安全分级 + 高风险确认门（防误操作外发）
	│   ├── classifier.py             # 任务/意图分类（Agent 编排层）
	│   ├── executor.py               # 执行引擎（编排工具/技能调用）
	│   ├── utils.py                 # 跨平台工具函数
	│   ├── agent_config.py          # Agent 配置管理（YAML + 环境变量覆盖）
	│   ├── llm_client.py            # LLM 客户端（离线脑 + OpenAI 兼容）
	│   ├── agent.py                 # Agent ReAct 循环
	│   └── conversation.py          # 对话记忆（滑动窗口 + 多模态支持）
├── mock/
│   ├── __init__.py              # Mock统一入口
│   ├── mock_tools.py            # 57个Mock工具（含系统管理）
│   └── mock_skills.py           # 24个Mock技能（已补全）
├── tests/
│   ├── __init__.py
│   ├── test_tool_registry.py    # 92个测试（全部工具+新工具+异常+别名+错误码）
│   ├── test_skills.py           # 43个测试（基础+扩展+搜索+Agent）
│   ├── test_os_service_api.py   # 11个测试（已扩展）
│   ├── test_app_tools.py        # 19个测试（应用操作含 click_at + 浏览器结构化契约）
│   ├── test_agentic.py          # 19个测试（安全分级/确认门/执行引擎/任务分类）
│   ├── test_agent_config.py    # 6个测试（配置加载/环境变量覆盖/类型转换/合并）
│   ├── test_conversation.py    # 28个测试（对话记忆/滑动窗口/多模态/序列化/多会话管理）
│   ├── test_llm_client.py      # 30个测试（离线脑匹配/拒绝/回退/工厂/响应格式/JSON兜底）
│   ├── test_agent.py           # 16个测试（Agent循环/多工具tool_call_id配对/确认门/最大步数）
│   ├── test_mock.py             # 33个测试 + index/archive/扩展技能/浏览器Mock冒烟
│   └── test_web_http.py          # 11个测试（HTTP 联网搜索 web_search 解析/清洗/重试/异常，离网）
├── examples/
	│   ├── agent_cli.py              # Agent 对话入口（CLI 交互界面）
│   ├── miniyu_web.py              # Web 桌面客户端（Flask，深色主题，异步确认模态框）
│   ├── live_llm_check.py         # 真实 LLM（qwen3.5-plus）真机核验脚本
	│   ├── qq_send_message_demo.py  # 真实QQ全自动发消息（点击+探针+OCR门）
│   ├── browser_doubao_demo.py   # 免登录豆包游客对话（真实鼠标聚焦+insertText+回车）
│   └── browser_deepseek_demo.py # 真实Edge给DeepSeek发消息（CDP demo，需登录）
├── docs/
│   ├── 第4组接口文档.md            # 接口文档（检查点 1）
│   ├── 第4组设计文档.md            # 设计文档（检查点 2）
│   ├── 第4组单元测试报告.md        # 单元测试报告（检查点 2）
│   ├── 第4组调研报告.md + .pdf     # 调研报告初稿（原 MCP 调研并入第 4 章）
│   ├── Agent编排层设计方案.md    # Agent 编排层设计方案（已实现：真实 LLM 阿里云百炼 qwen3.5-plus 已真机联调，见 docs/evidence/agent_qwen_live.md）
│   ├── evidence/                # 真机验证截图取证（清单见其 README）
│   │   ├── README.md            # 图→来源 demo→取证点
│   │   ├── agent_qwen_live.md       # Agent 真实 LLM（qwen3.5-plus）真机联调记录
│   │   ├── qq_group_一中兄弟会_读取聊天.png  # 打开群聊读取真实内容（OCR 取证）
│   │   └── qq_group_一中兄弟会_已发送.png    # 据实回复已入群、输入框清空（OCR 取证）
│   └── adr/
│       ├── 0001-sort_directory-tool.md  # 排序工具决策记录
│       ├── 0002-index-files-tool.md     # 索引工具决策记录
│       ├── 0003-archive-directory-tool.md  # 归档工具决策记录
│       ├── 0004-manage-archive-tool.md  # 压缩/解压一体重塑记录
│       ├── 0005-skill-library-extension.md # 技能库扩展记录
│       ├── 0006-search-modes.md         # 搜索精确/模糊匹配增强记录
│       ├── 0007-path-naming-unification.md # 命名归位与隐藏别名决策
│       └── 0008-app-send-message-click-probe.md # app_send_message 点击+探针+OCR门重写记录
├── .claude_backup_0509/          # ⚠️ 改前快照，非源码不参与运行：2026-09-05 重构前对 5 个 core/mock 文件的备份，
	│   │                            #     现有代码已验证、基本用不上，留到演示/答辩后整目录删除即可；用途详见其内 README.md
	├── config.yaml                  # Agent 配置文件（用户可直接编辑，不改代码）
	├── main.py                      # Demo入口
├── README.md                    # 已更新
├── requirements.txt             # 新增
├── 本组项目任务.md
├── 报告素材.md                  # 可写入报告的内容汇总（按章节摘取即可）
├── 设计任务书.pdf
└── 第4组项目进度跟踪.md          # ⬅️ 本文件
```

---

## 九、下周（第3周）准备（已收尾：原"与第3组联调"等计划已转为第4组自研自证，Agent 编排层 + 真实 LLM 已于 09-06 完成真机联调）

1. **与第3组联调** — 确认接口契约，使用Mock进行联调测试
2. **联调测试报告** — 记录联调过程和结果
3. **集成演示视频** — 准备第3周检查点材料

---

## 十一、Agent 编排层（2026-09-06 新增）

### 11.1 设计目标

使第4组能够**独立演示完整链路**，不依赖任何其他组的 Agent 实现。用户输入自然语言指令 → Agent 自动决定调用哪些工具 → 执行并返回结果。

### 11.2 架构

```
用户输入 "帮我整理桌面"
    ↓
 Conversation（对话记忆，滑动窗口 20 条）
    ↓
 Agent（ReAct 循环）
    ├── LLMClient（抽象基类）
    │   ├── DeterministicBrain（离线脑，规则匹配，零依赖）
    │   └── OpenAICompatibleClient（真实 LLM，兼容 GPT/DeepSeek/豆包/Ollama 等）
    ↓
 OSServiceAPI（59 个工具 + 26 个技能）
    ↓
 实际执行
```

### 11.3 核心组件

| 组件 | 文件 | 说明 |
|------|------|------|
| 配置管理 | `core/agent_config.py` | YAML 配置文件 + 环境变量覆盖，用户不改代码 |
| 对话记忆 | `core/conversation.py` | 滑动窗口，支持多模态（图片），自动管理 tool_call_id |
| 离线脑 | `core/llm_client.py` → `DeterministicBrain` | 关键词匹配 20+ 意图，零依赖，默认兜底 |
| 真实 LLM | `core/llm_client.py` → `OpenAICompatibleClient` | 兼容 OpenAI 格式，支持视觉模型，调 /v1/chat/completions |
| ReAct 循环 | `core/agent.py` → `Agent` | 安全确认门、最大步数保护、GUI 操作截图回传 |
| CLI 入口 | `examples/agent_cli.py` | 交互式对话，支持 /help /reset /tools /provider /config 命令 |

### 11.4 配置方式

用户只需编辑 `config.yaml`，无需修改代码：

```yaml
llm:
  provider: deterministic           # 离线模式（默认，零依赖）
  # 或 provider: openai_compatible  # 接真实 LLM
  base_url: "https://api.openai.com/v1"
  api_key: "sk-xxx"
  model: "gpt-4o-mini"
  supports_vision: false
```

环境变量优先级更高，可临时覆盖：

```bash
set AGENT_LLM_PROVIDER=openai_compatible
set AGENT_LLM_BASE_URL=https://api.deepseek.com
set AGENT_LLM_MODEL=deepseek-chat
```

### 11.5 离线脑匹配策略

使用**最长关键词优先**匹配策略，而非简单顺序匹配。例如"结束进程 1234"优先匹配"结束进程"（len=4）而非"进程"（len=2），确保更精确的意图识别。

### 11.6 测试覆盖

43 个新增测试覆盖：配置加载、环境变量覆盖、对话记忆全操作、离线脑 15 种意图匹配、Agent 完整循环、高危操作确认拒绝、最大步数保护。全部通过。**总计 415 个测试（全绿）**（测试覆盖见上文 §五 各文件清单：Web 模型切换、Agent 可调组合技能、截图理解/产物生命周期、统一视觉源 core.vision_bridge、邮件全链路 send_email、授权档位三档、QQ『先读后回』read_qq_chat + 联网白名单、HTTP 联网搜索 web_search 等方向均含用例）。

### 11.7 使用方式

```bash
# 离线模式（默认，零依赖）
python examples/agent_cli.py

# 真实 LLM 模式
set AGENT_LLM_PROVIDER=openai_compatible
set AGENT_LLM_BASE_URL=https://api.openai.com/v1
set AGENT_LLM_API_KEY=sk-xxx
set AGENT_LLM_MODEL=gpt-4o-mini
python examples/agent_cli.py
```

---

## 十二、进度日志

| 日期 | 工作内容 |
|------|----------|
| 2026-07-16 | 全面审阅代码和文档，建立进度跟踪文件 |
| | ✅ 完成MCP协议调研（已并入 `docs/第4组调研报告.md` 第 4 章，原独立报告归档删除） |
| | ✅ 补全Mock模块（13工具+4技能全量Mock） |
| | ✅ 补充测试覆盖（13→65个测试） |
| | ✅ 实现工具调用统计（get_stats） |
| | ✅ 代码风格整理（空行、docstring、空目录清理） |
| | ✅ 更新README、添加requirements.txt、撰写测试报告 |
| | ✅ 跨平台兼容改造（core/utils.py + safe_print + 系统自适应路径）|
| 2026-07-16 | 新增排序文件能力 |
| | ✅ 新增 `sort_directory` 工具（工具数 13→14）：按 name/size/mtime 排序，支持升降序 |
| | ✅ 返回恒定四字段 `{name,type,size,mtime}`；目录 size 为 None；目录优先；相等时按 name 兜底 |
| | ✅ `list_directory` 保持不变，排序逻辑抽取为私有 `_sorted_items` |
| | ✅ Mock 同步新增 `sort_directory`（MockToolRegistry） |
| | ✅ 新增 tests/test_sorting.py（16个测试），测试总数 65→81 |
| | ✅ 新增 ADR docs/adr/0001-sort_directory-tool.md 记录契约与决策 |
| | ✅ 更新 README、main.py demo（按大小降序演示） |
| 2026-07-16 | 新增索引文件能力 |
| | ✅ 新增 `index_files` 工具（工具数 14→15）：递归收集文件元信息 |
| | ✅ 支持 persist/load：索引写入 JSON、加载时无需遍历文件系统 |
| | ✅ 条目结构恒定 {path,name,type,size,mtime}；目录 size 为 None |
| | ✅ Mock 同步新增 `index_files`（MockToolRegistry） |
| | ✅ 新增 tests/test_indexing.py（12个测试），测试总数 81→94 |
| | ✅ 新增 ADR docs/adr/0002-index-files-tool.md 记录契约与决策 |
| | ✅ README 增加工具说明与示例；main.py 增加演示 |
| 2026-07-16 | 新增归档工具 |
| | ✅ 新增 `archive_directory` 工具（工具数 15→16）：打包目录为 zip |
| | ✅ 底层用 shutil.make_archive，root_dir=src；dest 自动补 .zip，未指定用 {目录名}_archive.zip |
| | ✅ Mock 同步新增 `archive_directory`（MockToolRegistry） |
| | ✅ 新增 tests/test_archive.py（7个测试），测试总数 94→102 |
| | ✅ 新增 ADR docs/adr/0003-archive-directory-tool.md 记录契约与决策 |
| | ✅ README 增加工具说明与示例；main.py 增加演示 |
| 2026-07-16 | 归档工具重塑为 manage_archive（压缩/解压一体） |
| | ✅ `archive_directory` 改名 `manage_archive`，新增 `action` 参数（compress/extract） |
| | ✅ 解压默认目标 {zip名}_extracted；目标已存在报错（防覆盖） |
| | ✅ Mock 同步改名并支持压缩/解压双模式 |
| | ✅ test_archive.py 重构为 manage_archive 测试（7→12个），总数 102→107 |
| | ✅ 新增 ADR docs/adr/0004-manage-archive-tool.md 记录重塑决策 |
| | ✅ README/main.py 更新为 manage_archive（压缩+解压演示） |
| 2026-07-16 | 技能库扩展（4→12 个技能） |
| | ✅ 新增 8 个技能：find_large_files / find_recent_files / summarize_files / cleanup_by_type |
| | ✅ 新增 batch_archive / duplicate_finder / rebuild_index / query_index |
| | ✅ Mock 全量补齐（mock_skills.py 12 个技能） |
| | ✅ 新增 tests/test_skills_extended.py（17个测试），测试总数 107→125 |
| | ✅ 新增 ADR docs/adr/0005-skill-library-extension.md 记录决策 |
| | ✅ README 增加技能表与 8.4 扩展技能示例 |
| 2026-07-16 | 搜索能力增强（精确/模糊匹配） |
| | ✅ `search_file` / `query_index` 新增 `mode` 参数：substring/exact/wildcard/regex |
| | ✅ 匹配逻辑抽取为 `_name_matches`（复用），用 fnmatch/re 标准库实现 |
| | ✅ 默认 substring 保持向后兼容；非法 mode 报错 |
| | ✅ 新增 TestSearchModes（10个测试），测试总数 125→135 |
| | ✅ 新增 ADR docs/adr/0006-search-modes.md 记录决策 |
| | ✅ README 更新技能说明与搜索示例 |
| 2026-07-16 | 合并技能测试文件 |
| | ✅ 将 test_skills_extended.py 并入 test_skills.py（单一技能测试文件，含基础/扩展/搜索模式） |
| | ✅ 删除 test_skills_extended.py，测试总数不变（135） |
| 2026-07-16 | 合并工具测试文件 |
| | ✅ 将 test_sorting/test_indexing/test_archive 并入 test_tool_registry.py（61个测试：全部工具+sort/index/archive） |
| | ✅ 删除三个旧测试文件，测试总数不变（135） |
| 2026-07-16 | 能力地图补齐（文件管理闭环） |
| | ✅ 工具 16→31：新增 create_file / metadata / checksum / compare / batch / disk_usage / find_* / 图片 |
| | ✅ 技能 12→20：新增 trash/restore/empty_trash/safe_delete/deduplicate/backup/smart_organize |
| | ✅ Mock 全量同步（31 工具 + 20 技能） |
| | ✅ 测试 135→164，全部通过 |
| | ✅ requirements.txt 标注可选 Pillow（图片工具） |
| 2026-07-16 | 异常处理补全（安全默认） |
| | ✅ copy/move/rename 增加 overwrite（默认拒绝覆盖已存在目标，平台行为统一） |
| | ✅ create_file/create_folder 增加 exists 语义（keep/overwrite/error） |
| | ✅ read_text_file 增加大小上限保护 |
| | ✅ batch_copy/batch_move 增加整体预检查（避免部分执行后失败） |
| | ✅ 测试 164→174，全部通过 |
| 2026-07-16 | 工具命名归位（文件/目录词形统一 + 隐藏别名） |
| | ✅ move_file→move_path、rename_file→rename_path、get_file_metadata→get_path_metadata |
| | ✅ create_folder→create_directory、delete_folder→delete_directory、get_folder_metadata→get_directory_metadata |
| | ✅ 旧名保留为隐藏别名（call 可用、list_tools 不显示）；delete_file/copy_file 保持仅文件（安全） |
| | ✅ 新增 CONTEXT.md 术语表 + ADR 0007 记录决策，更新 README/进度 |
| | ✅ 新增别名测试，测试 174→176，全部通过 |
| 2026-07-16 | 契约错误码补全（与第3组 ToolClient 对齐） |
| | ✅ ToolRegistry/Mock 未注册补 TOOL_NOT_FOUND、异常补 TOOL_ERROR |
| | ✅ SkillLibrary 补 SKILL_NOT_FOUND / SKILL_ERROR |
| | ✅ 新增 error_code 断言测试，测试 176→177，全部通过 |
| | ✅ README 补充《错误码约定》节 |
| 2026-09-05 | 创新点：应用操作能力（软件控制） |
| | ✅ 新增 core/app_controller.py：BaseAppController 抽象基类 + Windows/Linux 双实现 + 工厂 |
| | ✅ 工具 31→37：新增 list_windows / find_window / activate_window / send_hotkey / send_text / take_screenshot |
| | ✅ 技能 20→22：新增 app_open / app_send_message |
| | ✅ Mock 全量同步（mock_tools / mock_skills 补齐应用操作） |
| | ✅ 新增 tests/test_app_tools.py（10个测试），测试总数 177→187，全部通过 |
| | ✅ 真实 QQ 桌面端端到端验证 app_send_message（搜“一中兄弟会”群并发送“你好”） |
| | ✅ 修复 find_old_files 边界：st_mtime 比较 `<` → `<=`，days=0 可命中刚创建文件 |
| 2026-09-05 | 创新点：浏览器结构化控制（CDP） |
| | ✅ 新增 core/browser_controller.py：基于 CDP 直连浏览器内核，快照索引 + 索引操作 + Input.insertText 中文输入 |
| | ✅ 工具 37→46：新增 browser_launch/close/navigate/snapshot/click/type/read_text/screenshot/wait |
| | ✅ 技能 22→24：新增 browser_search（结构化网络搜索）、browser_extract（结构化阅读） |
| | ✅ tool_registry / skill_library 增加惰性 `browser` 属性（首次调用才实例化） |
| | ✅ Mock 全量同步（mock_tools 46 工具 / mock_skills 24 技能） |
| | ✅ 新增浏览器工具/技能 mock 契约测试，测试总数 187→197，全部通过 |
| | ✅ requirements.txt 新增 websocket-client 依赖（CDP WebSocket 通信） |
| | ✅ 参考开源设计：browser-use 的索引清单、agent-browser / cdp-browser-mcp 的可访问性树快照、LiteWebAgent 的 Sets-of-Marks |
| 2026-09-05 | 真实软件自动化落地：app_send_message 重写 + QQ/DeepSeek 真机验证 + 示例与文档沉淀 |
| | ✅ 工具 46→47：新增 click_at（屏幕左键单击）；控制器新增 read_clipboard / get_window_rect / set_clipboard（探针定位用，不对外注册） |
| | ✅ 证伪并重写 app_send_message：旧 Ctrl+F/Enter 路线在真实 QQ 落资料卡（此前记录的“验证成功”实为误报）→ 改“点击+探针+可选 OCR 标题门”，ADR 0008 |
| | ✅ 探针串污染 bug 发现并修复：命中后即 Ctrl+A+Delete 清场；修复后 217 测试全过 + 真机 OCR 复验搜索框干净 |
| | ✅ 真实 QQ 全自动向安全目标“我的手机”发送成功（截图取证，verify_result=True，OCR 门确认会话标题=目标） |
| | ✅ examples/ 新增：qq_send_message_demo.py（含 OCR 门）、browser_deepseek_demo.py（CDP） |
| | ✅ 真实 Edge 给 DeepSeek 发“你好”并读到回复（CDP 一次性验证）；边界：默认档案 ≥136 禁远程调试、DeepSeek 会话 cookie 不跨进程（见 4.8） |
| | ✅ 测试 197→217：test_app_tools 18→19 + 新增 test_agentic 19（安全分级/确认门/执行引擎/任务分类），全部通过 |
| | ✅ 浏览器示例改"免登录"目标：豆包游客模式实测通过（全新档案=新设备、额度重置）；新增 examples/browser_doubao_demo.py；关键发现：contenteditable 输入条必须真实鼠标点击聚焦，仅 JS focus 后 Enter 不发送。README / 进度跟踪 / 报告素材同步 |
| 2026-09-05 | 真实群聊“读后回”端到端：QQ 群“一中兄弟会” | 视觉 OCR 读取群聊近期内容（绿龙/猫娘/柔韧性等闲聊）→ 依聊天信息回一条不编造的短回复（OCR 标题门确认命中目标群）→ OCR 复验消息入群、输入框清空 |
| 2026-09-05 | 演示安全默认：examples/qq_send_message_demo.py 默认关键词 “一中兄弟会”→“我的手机” | 拿起来直接跑即发给自己（QQ 文件传输助手），不误发真实群聊；README 示例同步改为“我的手机”并注释说明 |
| 2026-09-05 | 规划：Agent 编排层设计稿（docs/Agent编排层设计方案.md） | 老师建议补“LLM 驱动的 Agent 循环”以自证完整；选定范围=方案1（LLM客户端+离线确定性脑+ReAct循环+Conversation+CLI）。**待 2026-09-08 问老师后再实现**，细节见设计稿第9节 |
| 2026-09-06 | **Agent 编排层全面实现** | |
| | ✅ 调研参考：MCP 协议规范（tools/list inputSchema）、LangChain @tool 装饰器、Microsoft UFO² HostAgent、OpenAI Function Calling / Anthropic Tool Use 格式 |
| | ✅ core/agent_config.py — YAML 配置管理 + 环境变量覆盖（6个测试） |
| | ✅ core/conversation.py — 对话记忆，滑动窗口，多模态图片支持，自动 tool_call_id 管理（8个测试） |
| | ✅ core/llm_client.py — LLMClient 抽象基类 + DeterministicBrain 离线脑（20+意图，最长关键词优先匹配）+ OpenAICompatibleClient（兼容GPT/DeepSeek/豆包/Ollama，含视觉模型支持）（15个测试） |
| | ✅ core/agent.py — Agent ReAct 循环，安全确认门，高危操作拒绝直返，最大步数保护，GUI操作截图回传（14个测试） |
| | ✅ examples/agent_cli.py — CLI 交互式对话入口，支持 /help /reset /tools /provider /config 命令 |
| | ✅ config.yaml — 用户可编辑的配置文件，内置注释说明 |
| | ✅ 测试 43 个新增，总计 217→260，全部通过 |
| | ✅ 更新 README / 第4组项目进度跟踪.md |
| 2026-09-06 | **miniyu 品牌化 + Web 界面 + 可移植性** | |
| | ✅ Agent 命名 miniyu（`core/agent.py` class Agent.name = "miniyu"） |
| | ✅ CLI 品牌化：miniyu ASCII logo、品牌提示符、美化输出 |
| | ✅ `examples/miniyu_web.py` — Web 桌面客户端（Flask，深色主题，异步确认模态框，轮询结果） |
| | ✅ 高危操作确认：CLI 弹确认提示，Web 弹模态框，用户可允许/拒绝 |
| | ✅ 可移植性：全部相对路径、`config.yaml` 自带注释、`run_miniyu_cli.bat` / `run_miniyu_web.bat` 一键启动 |
| | ✅ 测试：43 个 Agent 测试全部通过 + 所有原有 217 个测试通过 |
| 2026-09-06 | **对话记忆增强：多会话管理 + 流式输出 + 记忆系统** | |
| | ✅ 调研主流对话客户端（ChatGPT/豆包/Trae）记忆系统：滑动窗口、多会话、持久化、流式输出 |
| | ✅ 调研开源项目（Open Interpreter / UFO² / AutoGPT / LangGraph / CrewAI / Open WebUI）对比分析 |
| | ✅ SessionManager 多会话管理：创建/切换/删除/重命名/持久化（JSON 文件存储） |
| | ✅ 流式输出（SSE）：`chat_stream()` 支持逐 token 产出，兼容 OpenAI SSE 格式 |
| | ✅ 记忆压缩：`_compress_memory()` 超窗口时自动用 LLM 总结（对标 AutoGPT） |
| | ✅ 规划层：`_plan()` 复杂任务分解为子步骤（对标 AutoGPT 的 planning + execution 分离） |
| | ✅ 人类中断：`set_interrupt_handler()` 支持 human-in-the-loop（对标 LangGraph） |
| | ✅ Web UI 会话侧边栏：新建/切换/删除会话，对标 ChatGPT 的会话列表 |
| | ✅ CLI 会话命令：`/new` `/sessions` `/switch` `/rename` `/delete` |
| | ✅ 修复预存测试失败（工具计数 47→57） |
| | ✅ 新增 13 个 SessionManager 测试 + 5 个 Conversation 序列化测试 |
| | ✅ 测试 260→277，全部通过 |
| 2026-09-06 | **Agent 编排层真实 LLM 真机验证落地（修复 5 个"真机才触发"缺陷）** | |
| | ✅ 修复：请求无 system prompt / 流式纯文本永不终止（run_stream 一轮内补 stop 终止块）/ 多工具 tool_call_id 全部贴第一个 id / 无 JSON 文本兜底 / base_url 缺 `/v1` + 百炼未填 model 误用 gpt-4o-mini |
| | ✅ 新增 20 个单测，测试 277→297 全部通过（test_llm_client +15 / test_conversation +3 / test_agent +2） |
| | ✅ 真机 qwen3.5-plus（阿里云百炼 DashScope compatible-mode）跑通：创建报告文件夹 → 写摘要.txt → 列出目录 → 查磁盘，多步文件系统工具调用 |
| | ✅ 驱动 examples/live_llm_check.py 独立核验 5/5（非流式与流式均通过），证据 docs/evidence/agent_qwen_live.md |
| | ✅ API key 经环境变量注入，未写入仓库任何文件 |
| 2026-09-06 | **Web 端模型切换 + 切换后连通性实测（顶栏下拉框）** | |
| | ✅ GET /api/models 列出当前 API 可用对话模型（真机实测 192 个；过滤 embedding/rerank/文生图等非对话模型，拉取失败回退常用候选） |
| | ✅ POST /switch-model 先发最小 chat 实测连通：通 → 写回 config.yaml 的 llm.model + 热切换运行中 agent（当前会话不丢）；不通 → ❌ 提示原因、当前模型保持不变 |
| | ✅ UI 顶栏模型下拉框：点选即切换，弹绿(成功)/红(失败)toast；离线/未配置时禁用并提示先接入 API |
| | ✅ 真机三分支验证：切到平台聚合的 MiniMax-M2.1 成功并通过该模型完成对话；切回 qwen3.5-plus 成功；切不存在模型得 HTTP 404 → 提示失败且模型未变；离线模式两接口给出接入指引 |
| | ✅ 新增 12 个单测（tests/test_model_switch.py），测试 297→309 全部通过 |
| 2026-09-06 | **Agent 可调组合技能接线：模型能直接调用「QQ 搜索+发送」技能** | |
| | ✅ 根因：Agent 只把 ToolRegistry 57 个底层工具暴露给 LLM（list_tools_openai），app_send_message 是 SkillLibrary 技能、模型看不见也调不到 → 真机里模型退化成激活窗口+输字零散原语，可能发到错误会话 |
| | ✅ core/skill_library.py：新增 Agent 白名单 _AGENT_SKILL_SCHEMAS + openai_skill_names/is_agent_skill/list_openai_tools，把 app_send_message 转成 OpenAI function（含 schema，不暴露 Python 闭包 verify，改暴露 verify_ocr）；技能加 verify_ocr/vision_js，verify_ocr=True 且无 verify 时由 make_ocr_verify 内部构造「屏幕 OCR 门」（裁剪聊天标题横带交项目内视觉桥 core.vision_bridge 核对，找不到视觉源明确报错、绝不盲发第一行；外部 node 桥仅作显式 vision_js 的旧通道） |
| | ✅ core/os_service_api.py + core/agent.py：run/run_stream 给模型的 tools = 57 底层工具 + 白名单技能；_execute_one 拆成 _execute_tool / _execute_skill（HIGH 走确认门；app_send_message 默认强制 verify_ocr=True）；system prompt 加「QQ 发消息必须用 app_send_message、绝不拆零散步骤」守则 |
| | ✅ mock 层同步（MockSkillLibrary/MockOSServiceAPI 接口对齐）；新增 13 个单测（tests/test_agent_skills.py，不碰真 QQ / 真实视觉桥），测试 309→322 全部通过 |
| 2026-09-06 | **Agent 截图理解（视觉双通道）+ 过程产物生命周期** | |
| | ✅ 根因：config.supports_vision=false 关掉视觉 + _capture_and_add_image 把截图"文件路径字符串"当 base64（坏图）+ 模型没有主动"截图看一眼"入口 → 复杂/出错场景只会回"处理不了" |
| | ✅ qwen3.5-plus 原生多模态真机探测通过（内联真实 PNG 回 OK；此前 400 image format illegal 是发了一张 1×1 假 PNG）；config.supports_vision=true |
| | ✅ 修坏图 bug：截图先落盘→读文件→真 base64，作为独立"截图观测"消息（conversation.add_observation_image）在 tool 结果之后补入；GUI 工具成功/失败都自动补图 |
| | ✅ 新增 Agent 可调能力函数 screen_inspect(question)（只读、无确认门，非 SkillLibrary 技能、不改 57/24 计数）：视觉模型直接看图；无视觉模型走项目内视觉桥（config.yaml 的 vision_bridge 段）转文字，缺视觉源明确报错；system prompt 加规则 7「不确定/出错先 screen_inspect，不许直接说做不了」；模型可见函数 = 57 工具 + app_send_message + screen_inspect（共 59） |
| | ✅ 过程产物单独建会话产物目录（artifacts_root = AGENT_ARTIFACTS_DIR > config.memory.artifacts_dir > %TEMP%\miniyu_artifacts；截图在 <会话>/shots/，可被下一技能复用＝同学建议的"产出分离+流程化"）；用户说「清理截图/清理产物」直接清空当前会话产物；/reset 联动清空；本轮产生产物时回复末尾附一句"可清理"提醒 |
| | ✅ 新增 10 个单测（tests/test_agent_vision.py），测试 322→332 全部通过；规范见 ADR 0009；README/报告素材/设计方案/使用说明 已同步 |
| 2026-09-06 | **视觉桥可移植化：项目内 vision_bridge + config.yaml.example（不再依赖本机外部 node 脚本）** | |
| | ✅ 核心：新增 core/vision_bridge.py —— 统一"看图/OCR 视觉源"。llm.supports_vision 决定用谁：true → 直接用主对话模型读图（不必第二个 key）；false → config.yaml 顶层 vision_bridge 段（独立第二个视觉 API，可与主对话不同 key/厂商）。describe_image(image, question) 自动选源；两源都没有/请求失败/缺文件 → 抛 VisionBridgeError 并给 config 指引，绝不瞎编 |
| | ✅ skill_library.make_ocr_verify：vision_js=None 默认走项目内桥（require_vision 快速校验 + describe_image 读标题条），QQ 发送前 OCR 门跟随 supports_vision 选源；显式 vision_js / AGENT_VISION_JS 仍走外部 node 旧通道。agent screen_inspect 文字通道改调 _vb.describe_image（无视觉主模型场景必然选中 vision_bridge 段） |
| | ✅ 可移植：删除 examples/qq_send_message_demo.py 里本机 ~/.claude/qwen-vision vision.js 绝对路径引用；config.yaml 加 vision_bridge 段（本机先与 llm 共用同一把 dashscope key 注释说明可换独立视觉 key）；新增 config.yaml.example 模板（llm / vision_bridge 两个 key 留空 + 两种填法注释） |
| | ✅ 测试：test_agent_vision.py 覆盖统一视觉源分发（主 llm 有视觉→llm 段 / 纯文本→vision_bridge 段 / 无源报错 / provider error / 缺图文件）；test_agent_skills.py 增「主 llm 有视觉不配 bridge 也能 OCR」。两文件单跑 34 通过，全量 **332→343 全绿** |
| 2026-09-06 | **邮件全链路：自验证技能 send_email（SMTP 发信 + IMAP 回读核验）** | |
| | ✅ 方案：做成 1 个"自验证技能"（镜像 app_send_message），不是加底层工具——工具 57 不变、技能 24→25；新增纯 stdlib 协议层 core/email_client.py（smtplib/imaplib/email，零新增依赖），skill 内部完成 发→验 编排，真实 LLM 一次 function-call 传用户级参数即可 |
| | ✅ 核验设计：发完自动 IMAP 回读发件箱『已发送』确认落库；config.yaml email 段配 verify_inbox（收件侧邮箱 IMAP）→ 再轮询收件人收件箱核验『确实到达』（双端闭环）；回读未命中如实报 found=False，不把没验到当成功 |
| | ✅ fail-closed + 敏感：email 段没配好（缺 username/auth_code）在发之前抛 MailError 中文指引；授权码同 llm.api_key 敏感、只进 config/env 不入库（config.yaml.email 段入库留空）；QQ IMAP『已发送』在协议层是 modified-UTF-7（&XfJT0ZAB-），按解码名匹配已发送/Sent/… |
| | ✅ 接线：_AGENT_SKILL_SCHEMAS 白名单 + safety SKILL_META HIGH（对外不可撤回走确认门）+ SYSTEM_PROMPT 守则 8 + mock_skills 对等 + config.yaml(.example)/agent_config(ENV_MAP+DEFAULTS) 顶层 email 段 |
| | ✅ 测试：新增 tests/test_email.py 25 例（fail-closed / 协议层 SMTP·IMAP / 技能编排如实上报 / Agent HIGH 确认门 / Mock 对等），全量 **343→368 全绿**；**QQ 真机实跑**：主号→小号发"你好呀，小余人"，SMTP 接受 + **收件箱到达核验命中**；补 2 条回归（SELECT 加引号 / RFC2047 主题命中）→ **370 全绿**（test_email 25→27）。证据 docs/evidence/email_live_qq.md（复跑命令见 examples/email_demo.py --help） |
| 2026-09-06 | **QQ『先读后回』read_qq_chat（只读） + 联网白名单扩容（给模型"能读、能查"的上下文）** | |
| | ✅ 根因（真机反馈"不会变通"）：模型能"想"的范围只有上下文里出现的函数——QQ 之前只暴露能发的 app_send_message，没有"读"，它才说"send 技能只能发、不知道聊天历史"。能力没暴露＝对它不存在，不是它笨 |
| | ✅ 新只读技能 read_qq_chat：搜索进入目标会话 → OCR 核对会话标题（make_ocr_verify，目标不在结果前几行失败关闭、绝不读到别的会话）→ 截图裁聊天消息区 → 交 core.vision_bridge 视觉桥转写最近聊天返回 transcript/会话名/截图（缺视觉源明确报错，不假装读到）；safety SKILL_META READ_ONLY（任何档位不进确认门）；agent 对该技能默认强制 verify_ocr=True |
| | ✅ 白名单 2→5：app_send_message / read_qq_chat / send_email / browser_search / browser_extract；既有 browser_search/browser_extract（4.7 浏览器组合技能）提升为 Agent 可调函数；SYSTEM_PROMPT 加守则：QQ 找群看聊天再回＝先 read_qq_chat 再 app_send_message；联网查资料＝browser_search→browser_extract 并标注来源、不得编造；技能 25→26、mock 技能库/白名单对等 |
| | ✅ 测试：新增 tests/test_agent_read_web.py 11 例（暴露/READ_ONLY 无确认门/强制 verify_ocr/读多行重试/无行命中·无视觉源失败关闭/联网守则/mock 对等）+ test_agent_skills +2（read_qq_chat·联网 schema 只读）→ **391→404 全绿**（16 个测试文件） |
| | ✅ 联网搜索改 **HTTP 直连**（新增 core/web_search.py）：真机搜 CSGO 赛果"思考 3 分钟仍超时"→ 根因＝旧 browser_search 无头浏览器"打字+回车"**CDP 合成回车不触发提交**（词已打进框、URL 停在首页，wait_for 每次 10s 超时 × 模型重试）、百度另有安全验证墙；修复＝纯 stdlib HTTP 直连 **cn.bing.com** 结果页（避开 www 302 二次握手偶发重置 WinError 10054）再解析 b_algo {标题/链接/摘要}，**3 次连接级重试**兜底，实测 **~0.6s** 出 5 条；schema/engine 收敛仅 bing、FAQ 同步。全量 **404→415 全绿**（17 个测试文件，新增 tests/test_web_http.py 11 例，离网不联网） |
| 2026-09-07 | **FailoverClient 自动降级：主 API → 本地 Ollama → 确定性脑** | |
| | ✅ FailoverClient 类：按优先级依次尝试（主 LLM → 备用 LLM → 确定性脑），首次失败标记 degraded，每 5 分钟自动重试主 LLM |
| | ✅ 配置：`config.yaml` 新增 `llm.fallback` 段，含完整注释，支持 Ollama 本地模型零额外依赖 |
| | ✅ 零依赖：仅需 Ollama 独立进程（HTTP 连接），项目不捆绑任何 ML 库 |
| | ✅ 可移植性：Ollama 使用 winget 一键安装，模型通过 `ollama pull qwen2.5:1.5b` 下载（~1GB，CPU 可跑） |
| | ✅ 不配置 fallback 段时行为不变，向后兼容 |
| | ✅ CLI 启动显示降级状态（主模型/备用已就绪/已降级）；`/model` 命令显示当前使用的 Provider |
| | ✅ 新增 9 个 FailoverClient 测试（主/备用/兜底/流式/视觉继承/工厂函数），**全量 424 测试全部通过** |
| 2026-09-07 | **模型切换三连修：热切换失效 + 切模型后 400 + 本地模型"切而不换"（Web UI）** | |
| | ✅ 根因一（切换无效）：`/switch-model` 热切换只查 `_agent.llm.model`，但 FailoverClient 本身没有 `model` 属性（模型名在 `primary.model` 上）→ `hasattr` 恒 False、热切换被静默跳过，怎么切都还是原模型。修复：路由先判 `isinstance(llm, FailoverClient)` 走 `primary.model`，再回退普通 client；`/api/models`、`/api/fallback-models` 改为**从运行中的 Agent 读当前模型**（此前只读 config 文件，热切换后状态不同步） |
| | ✅ 根因二（切完模型对话报"Failed to fetch"）：真机复现抓包发现实为上游 API **HTTP 400** `messages with role "tool" must be a response to a preceding message with "tool_calls"`——对话历史残留工具调用消息，切到**不支持 function calling 的模型**（如 MiniMax-M2.1）后直接被拒。修复：`core/agent.py` 新增 `_sanitize_tool_messages()`，FailoverClient 场景发请求前把 `role:"tool"` 消息剔除、把带 `tool_calls` 的 assistant 消息降级为普通文本，任意模型可无缝接力历史 |
| | ✅ 根因三（本地模型"切而不换"）：切本地模型只改了 fallback 模型名，但 FailoverClient **主 API 可用时不走备用**→ 实际对话仍是云端 API 模型。修复：`core/llm_client.py` 新增 `force_local_mode()`（`_force_local` 标志，chat 时跳过主 API 直接走本地 Ollama，本地也挂才落确定性脑）；Web `/switch-fallback-model` 选本地模型即开启 force_local，`/switch-model` 选在线模型即关闭，往返切换语义正确；`status` 增加 `force_local` 字段，`Agent.model_name` 在该模式下如实返回本地模型名 |
| | ✅ UI：模型下拉框重写为**可搜索 Combobox**——输入即实时过滤（像浏览器地址栏），选项按 ☁️在线 API / 🖥️本地 Ollama 分组，点选即切换；Enter 选首个匹配、Esc 关闭、失焦自动收起。修点击不触发问题（`onmousedown` 改 `addEventListener('mousedown')`、`dataset.value` 改 `getAttribute('data-value')`）；fetch 加 `credentials:'same-origin'`；Flask 加 `@app.after_request` CORS 头 + OPTIONS 预检支持 |
| | ✅ 浏览器实测（内嵌浏览器全流程跑通）：在线三模型切换对话均正常（MiniMax-M2.1 / qwen-turbo / deepseek-r1-distill-qwen-7b 各自答出对应身份）；本地 `richardyoung/qwen2.5-7b-instruct-abliterated` 切换后**直接由本地模型作答**（~30s，CPU 推理）；本地→在线回切正常；`config.yaml` 模型列表更新为 `huihui_ai/qwen3-abliterated:4b` + `richardyoung/qwen2.5-7b-instruct-abliterated` 两个 Ollama 可拉取模型 |
| | ✅ 涉及文件：`core/agent.py`、`core/llm_client.py`、`examples/miniyu_web.py`、`config.yaml`；`miniyu使用说明.md` 同步更新 |
| 2026-09-07 | **工具可见性修复：换模型后 Agent"看不见"57 工具 + 文本协议自适应（Web UI）** | |
| | ✅ 根因（真机复现）：`agent.py` 的 ReAct 循环只在 `provider == "openai_compatible"` 时才传 `tools` 给 LLM——**配置 fallback 后 LLM 变成 FailoverClient（provider="failover"），工具列表根本没发给模型**，模型自然不会调用任何工具/技能（磁盘、QQ 发消息全部失效）。直连百炼实测确认：qwen3.5-plus / qwen-turbo / qwen3.6-flash / MiniMax-M2.1 均支持 function calling 且能正确发起 `app_send_message` 调用；**deepseek-r1-distill 系列不支持 tools 参数（HTTP 400 "The tool call is not supported."）**，切到这类模型时带工具请求直接被拒 |
| | ✅ 修复一（主 bug）：`agent.py` 的 `run()` / `run_stream()` 传 tools 的条件改为 `provider in ("openai_compatible", "failover")`，FailoverClient 分支工具正常透传（`FailoverClient.chat` 本就透传 tools，只是上游没传） |
| | ✅ 修复二（400 自适应）：`OpenAICompatibleClient` 新增两个运行时探测标志 + `reset_runtime_flags()`：① 模型不支持 tools 参数（如 deepseek-r1-distill）→ 自动切**文本协议模式**（tools-as-text：工具说明+JSON Schema 注入 system prompt，模型用 `<tool_call>{"name":...,"arguments":...}</tool_call>` 标记输出调用，客户端正则解析后走正常 ReAct 循环，对标 Qwen-Agent/早期 LangChain 对无 function calling 模型的通用解法）；② 历史残留 tool_calls/tool 消息被拒 → 自动 sanitize（剔除 tool role、tool_calls 降级文本）后重试，且后续请求预清理（须在构造 body 前处理，否则 body 仍引用旧列表——调试中发现的隐蔽 bug）。chat 与 chat_stream 双路径均适配；SSE 解析加 bytes 兜底 |
| | ✅ 修复三（多步上下文）：`_request_messages()` 不再无条件清理历史工具消息（原逻辑会让支持 function calling 的模型丢失"调用→结果"上下文，多步任务中途迷失）——改为完整下发，不支持时由 client 层收到 400 后按需降级，两类模型各得其所 |
| | ✅ 修复四（切换重置）：`FailoverClient` 新增 `switch_primary_model()`；主/备模型热切换后自动 `reset_runtime_flags()` 重新探测新模型的工具能力（Web `/switch-model` 路由同步） |
| | ✅ 浏览器实测：qwen3.6-flash（FailoverClient 模式）磁盘指令→真实磁盘数据 ✅、QQ 指令→完整执行 app_send_message 技能链路（搜索→核对→发送）✅；deepseek-r1-distill-qwen-7b 磁盘指令→不再 400，文本协议调用 disk_usage 拿到真实数据 ✅（7B 模型输出格式略糙属模型能力限制，链路本身已通） |
| | ✅ 新增 `tests/test_tool_protocol.py` 19 个测试（解析纯函数/400 自适应双分支/流式适配/Failover 透传/切换重置），**全量 443 全绿**；涉及 `core/agent.py`、`core/llm_client.py`、`examples/miniyu_web.py`、`tests/test_tool_protocol.py` |
| 2026-09-07 | **接入百炼服务端联网搜索（enable_search）：不自研搜索，用阿里云云端工具** | |
| | ✅ 决策依据（官方文档核实 + 真实 API 探针 `examples/probe_bailian_search.py` 五场景实测）：百炼 Chat Completions API 下联网搜索＝`enable_search: true`（云端透明执行、无 tool_calls 往返、非流式可用），qwen3.6-flash 在支持列表；**enable_search 与本地 62 个函数工具同请求混用无冲突**（场景 B/C 实测 200 正常，模型直接用注入的搜索资料作答）；对照组（场景 D）证实不开服务端搜索时模型确实会调本地 browser_search——回答了"本地工具会不会干扰模型选择"：会，所以生效期间必须隐藏。网页抓取（需 `search_strategy: agent_max` + 思考 + 强制流式）与代码解释器（`enable_code_interpreter` + 强制流式）**Flash 系列仅支持 Responses API**，留二期（需换 qwen3.5-plus 等推荐模型） |
| | ✅ 配置：`config.yaml` 新增 `llm.bailian_tools.web_search` 开关（默认 true），仅当 base_url 指向百炼（`aliyuncs.com`，含 dashscope 兼容模式与 MaaS 专线）时生效，其他提供商自动忽略；注释含计费提醒与二期预留说明 |
| | ✅ 客户端层（`core/llm_client.py`）：`OpenAICompatibleClient` 新增 `server_tools` 参数，chat/chat_stream 构造 body 时按开关注入 `enable_search`；沿用运行时自适应思想——API 400 拒绝该参数（如切到不支持的模型）→ 置 `_server_search_rejected` 停注重试，`reset_runtime_flags()` 热切换模型后重新探测；`FailoverClient.server_web_search_active` 跟随主 LLM（备用 Ollama 无此能力），**降级/force_local 模式下返回 False** 让 agent 层自动还原本地搜索工具兜底 |
| | ✅ Agent 层（`core/agent.py`）：新增 `_visible_tools()`——服务端搜索生效时对模型**隐藏本地 browser_search**（每步动态评估，API 拒绝后下一步自动还原，无需重启），`_agent_openai_tools()` 保持返回全集（测试/统计引用不受影响）；`_request_messages()` 生效时追加 `_SERVER_SEARCH_HINT`（显式覆盖 SYSTEM_PROMPT 守则第 9 条的本地搜索指引，避免模型去调已隐藏的工具，实测还让"您提供的资料"这类措辞变自然） |
| | ✅ Web UI 零改动：两处 Agent 创建均走 `load_config()→create_llm_client()` 工厂自动透传；`/switch-model` 热切换保留客户端实例（server_tools 不丢）且 `reset_runtime_flags` 会重新探测新模型搜索能力 |
| | ✅ 实测：新增 `tests/test_bailian_tools.py` 16 例（工厂透传/端点识别/chat·stream 注入/400 自适应停用/reset 还原/agent 隐藏与提示/Failover 委托与降级语义），**全量 459 全绿**；**端到端真机**（`examples/e2e_bailian_search.py`）：qwen3.6-flash 问"杭州今天天气"→服务端搜索生效、browser_search 已隐藏（62/63 可见）、模型直接给出具体实时天气数据（未调任何工具）。已知小瑕疵：搜索偶命中搜索引擎缓存页，模型引用时可能带旧日期（如把今天说成 9 月 3 日）——搜索源数据新鲜度问题，非链路问题 |
| | ✅ 涉及文件：`config.yaml`、`core/llm_client.py`、`core/agent.py`、`tests/test_bailian_tools.py`、`examples/probe_bailian_search.py`（探针）、`examples/e2e_bailian_search.py`（端到端验证） |
| 2026-09-07 | **联网搜索总开关（DeepSeek 式：一个开关管所有联网能力）** | |
| | ✅ 动机与实测（`examples/probe_search_trigger.py`）：模型自己会判断要不要搜（知识题"解释递归/冒泡排序"未触发搜索，时效题天气/汇率才搜），但 `enable_search` 开着时**每次请求注入约 3K token 固定搜索指令**（知识题 prompt_tokens 24 → 3088）且搜索按次计费——不需要联网时关掉可省 token 和延迟，故做成用户可控总开关 |
| | ✅ 配置重构：`config.yaml` 删除 `llm.bailian_tools` 段，新增**顶层 `web_search.enabled`**（默认 true）——三态语义：开+百炼端点=服务端搜索（enable_search 注入）+browser_search 隐藏（被替代）+browser_extract 保留（深读配套）；开+非百炼端点=本地 browser_search/browser_extract 全可见，模型自行决定调用；关=完全离线，不注入、两个本地搜索工具都隐藏 |
| | ✅ Agent 层（`core/agent.py`）：`web_search_enabled` 总开关状态（构造时同步进 LLM 客户端 server_tools，保证 config 与外部传入 client 一致）；`_visible_tools()` 三分支裁剪；`_request_messages()` 三分支提示——服务端生效追加 `_SERVER_SEARCH_HINT`（browser_extract 保留可用的措辞修正）、总开关关追加 `_OFFLINE_HINT`（均显式覆盖守则第 9 条，引导模型如实说明离线而非编造）；新增 `set_web_search_enabled()` 运行时切换（联动 server_tools + 重置探测标志，每步动态评估即时生效） |
| | ✅ 客户端层（`core/llm_client.py`）：`create_llm_client` 读顶层 `web_search.enabled` 预置主 LLM 的 `server_tools`（fallback 客户端不带，天然正确） |
| | ✅ Web UI（`examples/miniyu_web.py`）：顶栏新增「🌐 联网: 开/关」切换按钮（激活态高亮，悬停说明三态语义）；`/status` 返回 web_search 状态；`POST /toggle-web-search` 运行时切换（不回写 config.yaml——会话内即时生效，重启恢复默认） |
| | ✅ 实测：`tests/test_bailian_tools.py` 扩到 19 例（工厂三态/离线双隐藏/三分支提示/运行时切换联动），**全量 462 全绿**；端到端真机（`examples/e2e_web_search_switch.py`）：开 62/63（藏 browser_search）→ 关 61/63（双藏+离线提示）→ 切回开还原；**关态下真机问"今天杭州天气"模型如实回答"离线模式无法获取实时数据"**，未调隐藏工具、未编造数据 |
| | ✅ 涉及文件：`config.yaml`、`core/agent.py`、`core/llm_client.py`、`examples/miniyu_web.py`、`tests/test_bailian_tools.py`、`examples/probe_search_trigger.py`（触发实测）、`examples/e2e_web_search_switch.py`（开关端到端） |
| 2026-09-07 | **本地模型纯对话模式（tool_free）+ Web SSE 流式 + DeepSeek 式思考展示** | |
| | ✅ 痛点（`examples/probe_local_latency.py` 实测）：本地 7B 模型纯 CPU 写 800 token 要 132s，超 120s 总超时直接失败；带 63 个工具 schema（约 7.7K token）时纯 CPU prompt eval 就要 33~150s。用户决策：本地模型只做日常对话、不接工具 |
| | ✅ 纯对话模式：`llm_client.py` 新增 `tool_free`（chat/chat_stream 静默忽略 tools 参数）；`FailoverClient.tool_free_active` 两触发——主 LLM 声明 tool_free，或降级/force_local 落到 tool_free 本地客户端；工厂 `create_llm_client` 对本地端点（localhost:11434）自动 tool_free=true；`config.yaml` fallback 段显式 `tool_free: true` + `timeout: 300`（附实测原因注释，流式不受总超时限制） |
| | ✅ Agent 轻量对话分支：`_chat_mode()` 检测 + `_CHAT_SYSTEM_PROMPT`（自述离线限制、建议切在线模型办工具类任务、不编造实时数据、长文直接开写）——本地模型不再背负 63 工具 schema 与 ReAct 循环开销 |
| | ✅ 思考通道捕获：`chat_stream` 解析 `reasoning_content`/`reasoning` 增量 → `ChatResponse(reasoning=…, finish_reason="reasoning")`，`agent.run_stream` 原样透传（qwen3 系有独立思考通道，qwen2.5 无则自然没有思考事件） |
| | ✅ Web SSE 全链路：`POST /chat` 立即返回 task_id，后台线程跑 `agent.run_stream` 逐 chunk 入线程安全事件队列；`GET /task/<id>/events` 推送 reasoning/token/tool_call/confirm/done/error，15s 心跳防断连；高危确认改 confirm 事件 + `POST /task/<id>/confirm` 应答（拒绝不再卡死工作线程）；done/error 后任务自动清理防泄漏；修复流式终态文本重复拼接 bug（stop chunk 改覆盖不追加） |
| | ✅ 前端 DeepSeek 式体验：思考过程实时滚动渲染 + 计时「(Xs)」→ 完成后折叠为「已深度思考（用时 Xs）」可展开回看；正文逐字渐进上屏 |
| | ✅ 测试：新增 `tests/test_tool_free_sse.py` 21 例（tool_free 忽略工具 / reasoning 双字段捕获 / Agent chat 分支 / Failover 自动激活 / SSE 队列管线含心跳与清理），**全量 462→483 全绿** |
| | ✅ 端到端真机（`examples/e2e_local_chat_stream.py`，**14/14 通过**）：场景 A force_local 切本地写故事——首 token 9~20s（原带工具 33~150s）、总耗时 59~63s 流式全程无超时、思考 1415~1504 字 + 正文 333~363 字；场景 B 主 API 连接层挂掉→自动降级（degraded=True + tool_free_active=True + 本地正常回复 100.8s）；场景 C Web SSE 434 个事件（reasoning×331→token×102→done）按序渐进到达、done 携带完整故事、任务无泄漏 |
| | ✅ 涉及文件：`core/llm_client.py`、`core/agent.py`、`examples/miniyu_web.py`、`config.yaml`、`tests/test_tool_free_sse.py`、`examples/e2e_local_chat_stream.py`、`examples/probe_local_latency.py`（延迟诊断） |
| 2026-09-07 | **本地模型乱码修复（UTF-8 增量解码）+「停止生成」打断功能（DeepSeek 式）** | |
| | ✅ 问题一（乱码）：三个本地模型前端回复全部变成 `æ°è½æº` 式乱码。根因（`examples/_probe_ollama_encoding.py` 实测）：Ollama 流式响应头 `Content-Type: text/event-stream` **不带 charset**，requests 对 `iter_content(decode_unicode=True)` 按推断的 ISO-8859-1 解码，UTF-8 中文被逐字节解成 mojibake——**不是模型输出乱码，是 Python 客户端解码层出错**（豆包的诊断方向正确，但落点在前端/后端转发层，实际在 `core/llm_client.py` 的 SSE 读取处） |
| | ✅ 修复（`core/llm_client.py`）：`chat_stream` 改按字节读流（`iter_content` 不带 decode_unicode）+ `codecs.getincrementaldecoder("utf-8")` 增量解码——同时天然容忍网络块边界切在中文 3 字节中间的情况；str chunk（测试桩/个别实现）原样直通 |
| | ✅ 问题二（无法打断）：本地小模型偶发死循环/超长输出时用户只能干等。修复三层：① `agent.py` `run_stream(stop_check=...)` 新增可选回调，轮次间隙/流式 chunk 间隙两处检查，触发时保留已流出文本并以 stop chunk 收尾（DeepSeek 式打断体验，会话历史含部分文本+停止标记且落盘）；② Web 端任务持有 `stop_event`，新增 `POST /task/<id>/stop` 置位端点（对已结束任务幂等拒绝）；③ 前端生成中"发送"按钮变红色「⏹ 停止」（可点击），点击后 POST stop 并显示"停止中…"，SSE done 后恢复 |
| | ✅ 测试：新增 `tests/test_stream_stop_encoding.py` 11 例（UTF-8 字节流/Latin-1 推断回归锁/多字节跨块/str 兼容 + stop_check 四场景 + stop 端点三场景）；`test_tool_free_sse.py` 的 `_FakeAgent` 桩同步 `stop_check` 签名，**全量 483→494 全绿** |
| | ✅ 端到端真机（`examples/e2e_stop_encoding_live.py`，需 Web+Ollama 运行）：三个本地模型全过——1.5B（首 token 4.4s / 总 7.2s）、4B 思考模型（首事件 30.3s，正文+思考流均无乱码）、7B（中文正常但首 token 267s，CPU 上不可用）；停止请求→终态均 <0.5s，终态含「⏹ 已手动停止生成。」+ 保留部分文本 |
| | ✅ 浏览器实测（内嵌浏览器）：流式中按钮正确变红「⏹ 停止」、中文逐字正常渲染无乱码；点击停止后部分文本保留 + 停止标记出现 + 按钮恢复「发送」；停止后立即发新消息对话正常（功能未被破坏）。注意：服务重启后旧标签页需硬刷新才会加载新版前端 JS |
| | ✅ 涉及文件：`core/llm_client.py`、`core/agent.py`、`examples/miniyu_web.py`、`tests/test_stream_stop_encoding.py`、`tests/test_tool_free_sse.py`（桩签名）、`examples/e2e_stop_encoding_live.py`（真机验证）、`examples/_probe_ollama_encoding.py`（诊断） |
| 2026-09-08 | **思考内容折叠（DeepSeek/Trae 式）+ 历史会话侧栏增强 + .bat 乱码修复 + 使用说明补 API/本地模型指引** | |
| | ✅ 思考折叠（`examples/miniyu_web.py` 前端）：思考面板此前只有 JS 没样式，现补全 CSS——完成后默认折叠成一行「已深度思考（用时 X 秒）▼」，点标题展开/再点收起；**展开后内容限高 300px + overflow-y 滚动**（`max-height:300px; overflow-y:auto`），一则很长思考不把整页撑高、用户在框内滑完全部。实测（浏览器 + 本地 qwen3-4b）：折叠→展开（300px 高、scrollHeight 1680px 可滚动）→再收起均正常 |
| | ✅ 每次启动新开对话（`core/agent.py`）：`load_all()`（历史载入内存供侧栏浏览/切换）+ `create()`（新开一个空对话作当前会话）替换原 `ensure_current()`。实测每次重启服务器都生成新的「新对话」会话，历史独立列出 |
| | ✅ 历史会话按最近访问排序 + 显示时间（`core/conversation.py` + Web）：`SessionManager.switch()` 切换时刷新该会话 `updated_at` 并落盘（访问即置顶）；侧栏每项标题下加一行小字「今天 HH:mm · N条」时间；列表后端已按 updated_at 降序；前端默认渲染最近 20 条，多出则显示「加载更多历史（N 条）」按钮展开全部 |
| | ✅ 切换会话回看历史（Web）：`POST /sessions/<id>` 现返回该会话的可读消息（user/assistant 正文，跳过工具/过程行），前端 `switchSession` 渲染回聊天区（顶部一条「—— 历史对话：标题 ——」系统提示）。修正旧行为：原来切换只加一句"已切换"提示、不显示历史内容。标题加 `escapeHtml` 防 XSS |
| | ✅ .bat 乱码修复（`run_miniyu_web.bat` / `run_miniyu_cli.bat`）：旧文件 UTF-8 中文 + `chcp 65001` 仍乱码——cmd 按系统旧代码页(GBK)预读批文件、chcp 无法回改已读字节。改成**纯 ASCII 英文内容**（含注释说明原因），彻底免疫编码；启动前 `cd /d "%~dp0"`。用任意编码双击都不会再出 `'潰' 不是内部或外部命令` |
| | ✅ 使用说明补全（`miniyu使用说明.md`）：新增醒目提示「首次使用/给别人/重新部署必看」——换自己的 API key 与模型（llm 段 base_url/api_key/model）、没 key 仅离线模式；新增「可选：使用本地 Ollama 模型」小节——下载安装 Ollama、`ollama pull` 指定模型（1.5b≈1.1GB 推荐 / 4b≈2.3GB 带思考）、`OLLAMA_MODELS` 改存储盘、网页模型框选本地模型步骤 |
| | ✅ **语音输入 🎤（浏览器原生 Web Speech API，零后端）**（`examples/miniyu_web.py` 前端）：输入框左侧新增麦克风按钮（输入框 flex 缩小留位），点击开始倾听（按钮变红脉动 + placeholder「🎤 正在倾听，请说话…」）、**再点关闭**；`interimResults` 边说边出、识别文字填入输入框由用户确认后发送；`lang=zh-CN`。兼容检测 `window.SpeechRecognition || webkitSpeechRecognition`：不支持（Firefox/Safari）直接隐藏按钮；`onerror` 分场景 toast（not-allowed 权限拒绝 / no-speech 没听到 / network 服务不可用 / audio-capture 无麦克风）；生成中禁用。调研结论（豆包三路线中选路线1）：Web Speech API 依赖浏览器厂商云端 ASR——Chrome 走谷歌（大陆常连不上）、**Edge 走微软 Azure（大陆可用）**，localhost 属安全上下文免 HTTPS，使用说明已注明"建议用 Edge + 隐私提醒" |
| | ✅ 全量测试 494/494 通过（会话相关 test_conversation.py/test_tool_free_sse.py 49 通过）；浏览器实测功能 A（历史侧栏：标题+时间显示、点击加载历史、访问置顶高亮）与功能 B（思考折叠+限高压顶可滚动）全部通过；语音 🎤 实测（TRAE Chromium 142 支持 API）：按钮出现于输入框左、点击红脉动倾听/再点恢复、无 JS 错误、网络语音服务不可用时红色 toast 降级提示符合预期 |
| | ✅ **语音输入体验修复**（`examples/miniyu_web.py`）：`continuous=false→true` 持续倾听（说话停顿不再终止，点 🎤 才关闭）；`onresult` 由覆盖改为**追加**（`_voiceBase` 保留输入框已有内容，多次语音结果与手动打字自然累积），全量重算保证幂等（e.results 累计不重复），interim→final 正确替换不重复。浏览器 Console 模拟验证：`帮我整理桌面`→`然后清理垃圾文件`→`最后查看磁盘空间` 逐次追加不覆盖 ✅ |
| | ✅ **历史会话自动标题**（`core/conversation.py`）：修复 `create()` 用 `title or "新对话"` 导致 `add_user` 的 `if not self.title` 永远不触发、自动标题从未生效的 bug——`create()` 默认空标题，首条用户消息到达时 `_auto_title` 取**第一句**（首个停顿标点前内容、超 20 字截断加 …，`re.split` 按 `。！？!?，,；;、：:…` 切分）生成；`from_dict` 加载旧会话时对空/"新对话"标题用首条 user 消息**自动迁移重命名**。不依赖 LLM（离线/确定性脑可用）。功能验证 5 项全过（空标题/标点截断/超长截断/旧数据迁移/显式标题保留），单测 67 通过、全量 494/494；浏览器端到端实测：旧会话已迁移成具体摘要（"今天天气怎么样"等）、新对话发消息后标题即时变为"帮我整理桌面"（与回复成败无关） |
| 2026-09-08 | **本地图片直读 read_image + 编程项目辅助（search_in_files/edit_file）+ Web 公式渲染（KaTeX 本地打包）+ 深浅主题切换** | |
| | ✅ **本地图片直读（`core/agent.py` + `core/vision_bridge.py`）**：用户反馈"让它看本地图片它说读不了、只能靠截图"——根因：模型可见函数只有 `screen_inspect`（截屏观察），**没有"给文件路径直接读图"的通道**；截屏能看≠能读文件（一个走屏幕像素、一个走文件 I/O，模型根本没有读本地文件的视觉工具）。新增 Agent 视觉能力函数 **`read_image(path)`**：`llm.supports_vision: true`（主模型有原生视觉）→ base64 原图直接发给主模型看；`false`（无视觉主模型）→ 自动走 `core/vision_bridge` 独立视觉 API 转文字描述；文件不存在/两个视觉源都没配好 → 明确报错不瞎编。模型可见函数全集 = **59 工具 + 5 白名单技能 + screen_inspect/read_image 视觉双通道（共 66）** |
| | ✅ **编程项目辅助（`core/tool_registry.py` + `core/safety.py` + `core/agent.py`）**：对照"真 AI 助手帮人改代码/查 bug"的能力缺口补齐两把工具——**`search_in_files`**（按内容搜关键词/正则，跨文件、带行号与原文，支持扩展名过滤、超时保护；区别于按文件名的 `search_files`）与 **`edit_file`**（在文件里找一段原文精确替换，默认替换第一处、可 occurrence 指定/`all` 全换，保留原编码，返回编辑摘要——比"整文件重写"安全得多）。SYSTEM_PROMPT 新增守则第 11 条「先侦察、后动手」：list_directory → search_files（按文件名）→ search_in_files（按内容/报错信息）→ read_text_file（大文件分段读）→ 理解后才 edit_file/write_text_file → run_command 实跑验证 → 中文总结改了哪些文件、为什么。`safety.py` 风险元数据：search_in_files=READ_ONLY、edit_file=MEDIUM |
| | ✅ **Web 公式渲染（`examples/miniyu_web.py` + `examples/static/vendor/`）**：用户反馈"数学公式（指数/根号）显示成 `$...$` 和反斜杠原文"——根因：大模型输出的是 Markdown+LaTeX 混合文本，前端没做渲染直接 `innerText` 输出。按豆包建议 + 开源同行方案（Open WebUI/Lobe-Chat 同款：marked + KaTeX auto-render）实现 `renderContent()`：**① 先提取 $...$/$$...$$ 片段为私有区占位符**（marked 会吞 `\frac` 的反斜杠，必须先保护——实测修复前块级公式渲染失败）→ ② marked 转 HTML → ③ 还原占位符 → ④ DOMPurify 消毒（防注入）→ ⑤ renderMathInElement 渲染公式；库 CDN 不稳定，**KaTeX/marked/DOMPurify 连同 KaTeX 字体已下载本地打包**到 `examples/static/vendor/`（Flask `/vendor/<path>` 路由托管，离线可用；CDN 作 onerror 兜底回退） |
| | ✅ **深浅主题（`examples/miniyu_web.py` 前端）**：顶栏「会话: N」徽章**左侧**新增 ☀️/🌙 主题按钮（用户点名要的位置）——CSS 变量化双主题（`:root` 深色默认 + `:root[data-theme="light"]` 覆盖变量），点击切换、`localStorage('miniyu-theme')` 记忆、`<head>` 首帧前应用防闪烁、加载完成后同步按钮图标。全部 UI 元素改用 `var(--*)`，无残留硬编码颜色 |
| | ✅ 测试：新增 `search_in_files`/`edit_file` 单测（TestProgrammingTools 类）与 `read_image` 视觉测试（`tests/test_agent_vision.py`：双通道 mock + 文件缺失报错），全量 **512 全绿**（59 工具断言同步更新 4 个测试文件） |
| | ✅ 浏览器实测（内嵌浏览器，localhost:5000）：① 主题按钮位于会话数徽章左侧，点击切换 data-theme、刷新后保持（localStorage 记忆生效）；② 注入含 `$A^2-3A-2E=O$`、`$$...$$`、`**加粗**`、`` `代码` `` 的测试消息——`katexCount=5`（4 行内 + 1 块级 `katex-display`）、`hasDollar=false`、strong/code 正常；③ vendor 四个库全部本地加载成功、无 404。修复过程记录：旧服务进程占用 5000 端口导致 /vendor 404（停旧进程重启解决）；vendor 路由首次报 `NameError: os`（`os` 未 import，改用已导入的 `pathlib.Path`）；`send_from_directory` 未从 flask 导入（补 import） |
| | ✅ 涉及文件：`core/agent.py`、`core/tool_registry.py`、`core/safety.py`、`examples/miniyu_web.py`、`examples/static/vendor/`（新增）、`mock/mock_tools.py`、`tests/test_agent_vision.py`、`tests/test_tool_registry.py`、`tests/test_agent_skills.py`、`tests/test_mock.py`、`README.md`、`miniyu使用说明.md` |
| 2026-09-08 | **接入百炼服务端代码解释器（code_interpreter）+ 前端布局锁定 + 工具调用折叠面板 + 性能调优（结果截断 / 连接池）** | |
| | ✅ **服务端代码解释器**（`core/llm_client.py` + `core/agent.py` + `config.yaml` + 探针 `examples/probe_bailian_code_interpreter.py`）：用户问"百炼官方提供的代码解释器用起来了吗"→ 调研官方文档 + 真实 API 探针实测确认启用方式。**关键结论**：Chat Completions 模式下启用参数是**顶层 `enable_code_interpreter: true`**（`tools` 数组里的 `{"type":"code_interpreter"}` 是 Responses API 写法，本端点会 400 `'function' is a required property`）；且**仅支持流式调用**（"Non-streaming mode does not support Code interpreter"）、**不能与本地函数工具同请求**（"Agent mode does not support tools"）、官方建议配 `enable_thinking`。据此 `_apply_server_tools()` 只在 `stream=True` 且本请求未带本地工具（纯计算/纯对话回合）时注入；开关 `config.yaml` 顶层 `web_search.code_interpreter: true`（默认开，注释含实测约束与计费提醒） |
| | ✅ **纯计算回合判定**（`core/agent.py`）：新增 `_PURE_CALC_RE`（计算/次方/数学/统计/解方程/π/阶乘…）+ `_LOCAL_REF_RE`（文件/桌面/QQ/浏览器/截图…）双正则——命中计算关键词且**不涉及本地资源**时，该回合以 `tools=None` 走服务端解释器（云端沙箱跑 Python，天然规避"与本地工具混用 400"）；涉及本地资源照常走本地工具；`_CODE_INTERPRETER_HINT` 追加进 system prompt（告知模型"纯计算直接给结论、不必调本地 run_command"）。实测 qwen3.7-flash / qwen3.5-plus 问「123的21次方」均返回**逐位精确**结果（77,269,364,466,549,865,653,073,473,388,030,061,522,211,723），而此前模型只能硬算/乱报 |
| | ✅ **拒绝自适应**（`core/llm_client.py`）：`_code_interpreter_rejected_by()` 识别三类 400（模型不支持含 "code_interpreter" / 与本地工具混用 "Agent mode does not support tools" / 非流式 "Non-streaming mode..."）→ 置 `_code_interpreter_rejected` 停注重试；`enable_thinking` 单独被拒则只丢思考参数（解释器依赖思考、一并停用）；`reset_runtime_flags()` 热切换模型后重新探测；`FailoverClient.server_code_interpreter_active` 跟随主 LLM（降级/force_local 返回 False，agent 层不再注入） |
| | ✅ **前端布局锁定**（`examples/miniyu_web.py`）：用户反馈"整个前端都能被划出用户界面"→ `html, body { overflow:hidden; overscroll-behavior:none }` + `.chat-container` 内部滚动、`overflow-x:hidden` 防横向溢出——页面整体**固定于视口**，滚动只发生在聊天区内部 |
| | ✅ **工具调用折叠面板**（`examples/miniyu_web.py`）：用户反馈"工具调用显示占据大片输出"→ 从"平铺一行行"改为**仿深度思考的折叠面板**——同一轮内多次调用归入同一面板、默认折叠为一行「工具调用 (N 个) ▼」，点标题展开/再点收起；展开后 `max-height:260px + overflow-y:auto` **限高滚动**，不再撑高页面 |
| | ✅ **性能调优**：① 工具结果**超长截断**（`core/conversation.py` `add_tool_result(max_chars=4000)` + `core/agent.py` `tool_result_max_chars` 配置，默认 4000，config.yaml 可调）——`run_command` 输出/文件内容等大结果不再全量回传 LLM，超出截断并附"可针对性再读"说明（对标 LangGraph 社区 TOOL_OUTPUT_MAX_TOKENS 实践，省 token 且加速请求）；② **HTTP 连接池复用**（`core/llm_client.py` `requests.Session` + `threading.local` 线程隔离，Session 非线程安全）——每次请求省 TCP/TLS 握手约 50-200ms（Web 端后台多线程流式场景收益明显） |
| | ✅ 全量测试 **512 全绿**（本轮纯增量、未改测试文件）；浏览器实测：页面无法再整体拖动/横向溢出、工具调用面板折叠→展开（260px 限高、内部滚动）→收起均正常；"123的21次方"真机走服务端解释器返回精确结果 |
| | ✅ 涉及文件：`core/llm_client.py`、`core/agent.py`、`core/conversation.py`、`examples/miniyu_web.py`、`config.yaml`、`examples/probe_bailian_code_interpreter.py`（探针）；README / 进度跟踪 / 报告素材 / 使用说明 同步更新 |
| 2026-09-09 | **跨平台可移植性落地（Windows + Ubuntu 双平台）+ 他人部署指南 + 语音输入浏览器要求** | |
| | ✅ **背景与评估**：用户指出任务书要求"在 Linux 桌面环境实现 Agentic OS"，但开发一直跑在 Windows。逐文件审查（`app_controller` / `browser_controller` / `utils` / `agent` / `conversation` / `config` / 启动脚本）确认核心代码本就**双平台**：全部相对路径 + `pathlib` 自动适配分隔符；临时目录进程产物用 `tempfile.gettempdir()`（Win %TEMP% / Linux /tmp）；编码全项目 UTF-8（`safe_print` 防 Win GBK 崩溃）；依赖 requirements.txt 全跨平台库；`app_controller` 分 `WindowsAppController`（ctypes Win32）+ `LinuxAppController`（xdotool/wmctrl/xclip/scrot）；`browser_controller` 用跨平台 CDP（`_find_chrome` 双平台路径探测）。→ **结论：可移植、能双平台用，无需改架构** |
| | ✅ **新增强化**：① `setup_linux.sh` 一键配置脚本（装系统依赖 xdotool/wmctrl/xclip/gnome-screenshot + pip 依赖 + 从 `config.yaml.example` 生成 `config.yaml` + **检测 Wayland/Xorg** 给可移植性指引）；② `run_miniyu_web.sh` / `run_miniyu_cli.sh`（Linux 启动脚本，等价现有 .bat）；③ `LinuxAppController.__init__` 检测 `XDG_SESSION_TYPE=wayland` 时提示切「Ubuntu on Xorg」（xdotool/wmctrl 是 X11 工具，Wayland 会话桌面自动化不可用，仅文件管理/Web 聊天不受影响）；④ `.gitattributes` 锁定 `*.sh` 为 LF、`*.bat` 为 CRLF（防 Windows 检出 CRLF 破坏 bash 脚本）；⑤ `config.yaml.example` 补齐 `web_search.code_interpreter` 新字段 |
| | ✅ **可移植性/发他人**：`config.yaml` 含个人敏感字段（llm.api_key + vision_bridge.api_key 两把百炼 key、email 两组 QQ 邮箱授权码）→ 已被 `.gitignore` + git skip-worktree 兜底**不入库**，随代码分发的是空 key 模板 `config.yaml.example`。他人部署流程写入 README/使用说明：`cp config.yaml.example config.yaml` → 填自己的 key / 模型 / Ollama 本地模型 → 启动，即可完整使用；个人 key 与本地模型不随代码分发 |
| | ✅ **语音输入浏览器要求**：明确写入文档——Web 语音输入用**浏览器原生 Web Speech API，仅 Chrome/Edge 支持**，正式推荐 Edge（走微软语音服务、国内可用），Chrome 走谷歌服务大陆常连不上；其它浏览器自动隐藏 🎤、打字不受影响 |
| | ✅ 更新记录文件：README（可移植性小节重写 + 近期变更 09-09 + 跨平台章节补 Linux 要点）、miniyu使用说明（新增「系统与浏览器要求」+ Linux 启动 + **新章节「7. 给别人部署」** + FAQ 语音/Linux/发他人类 4 条）、报告素材（新增可移植性章节）。若在 Ubuntu 虚拟机按新流程实测有出入可再迭代 |
| | ✅ 涉及文件：`core/app_controller.py`、`config.yaml.example`、`setup_linux.sh`（新增）、`run_miniyu_web.sh`/`run_miniyu_cli.sh`（新增）、`.gitattributes`（新增）、`README.md`、`miniyu使用说明.md`、`第4组项目进度跟踪.md`、`报告素材.md` |
| 2026-09-09 | **对照任务设计书补齐第 4 组提交文档（docs/）** | |
| | ✅ **通读设计书核对 5 组完整设计**：逐条对照"分组任务详解·第 4 组功能要求"确认**功能无缺失且超额**——ToolRegistry 统一调用（59 工具）、SkillLibrary 系统管理（26 技能）、调用统计 `get_stats()`（必做）、工具签名 `get_tool_signature`（可选也做了，SHA256）、参考 MCP 协议（`docs/第4组接口文档.md`，契约已并入接口文档）、Mock（mock/mock_tools.py 57 工具同构）。因此本轮**无代码改动、聚焦补齐可提交的文档**，以满足设计书评分/验收要求 |
| | ✅ **在 docs/ 新增 5 份提交文档**：① `第4组调研报告.md`（≥3000 字，UFO²/Tools 定位 + MCP 协议调研 + 竞品对标 + OS 持久性关联 + 参考文献，占 20 分）；② `第4组设计文档.md`（架构分析/接口契约 tool_result_json/模块设计 ToolRegistry·SkillLibrary·Mock/流程图 mermaid/可行性分析/风险评估，占 30 分）；③ `第4组单元测试报告.md`（512 全绿 + 设计书功能→测试映射）；④ `第4组联调与集成报告.md`（真实 LLM qwen3.5-plus function-calling + 邮件 SMTP/IMAP + 百炼服务端工具，设计书 6 演示场景核对、场景 6 整理下载目录端到端）；⑤ `第4组交付说明.md`（提交/打包清单、一键复现、验收自检、待办——提醒最终只发 `config.yaml.example` 不含个人 key） |
| | ✅ 涉及文件：`docs/第4组调研报告.md`、`docs/第4组设计文档.md`、`docs/第4组单元测试报告.md`、`docs/第4组联调与集成报告.md`、`docs/第4组交付说明.md`（均新增）；README / 进度 / 报告素材同步登记 |
| 2026-09-09 | **核查第 5 组职责 + 调研报告补字 + 汇报 PPT（检查点 4）** | |
| | ✅ **第 5 组（系统协调+安全+RAG）核对**：对照设计书逐项核查——安全层（safety.py 四层分级/授权档/确认门）已完整 ✅；**缺失 4 项**：独立 SystemCoordinator、RAG 向量检索、审计日志、第 5 组降级 Mock → **新增 `core/coordinator.py`** 补齐：SystemCoordinator 编排 + SecuritySandbox 四层安全（权限/沙箱/签名/隐私）+ RAGKnowledgeBase 轻量向量检索（词频+余弦，零依赖）+ AuditLog 审计（JSON Lines 可落盘）+ MockCoordinator/MockRAG/MockAudit 降级；**接入 agent.py**（run/run_stream 结束自动记审计+存 RAG，`agent.coordinator.enabled` 可关）；config.yaml.example 加 coordinator 段 |
| | ✅ **调研报告字数**：补「与第 5 组协同调研」+「MCP 演进」+「对标 Computer Use」+「调研局限与后续工作」章节，汉字 2453 → **约 4000 字达标**（≥3000）；并生成 `docs/第4组调研报告.pdf`（正文宋体小四，`docs/scripts/md_to_pdf.py` 一键复现，`.md` 保留） |
| | ✅ **汇报 PPT**：`docs/第4组汇报PPT.pptx`（**18 页 ≥15 页**，python-pptx 生成：封面/目录/UFO²定位/调研/架构/模块/安全RAG/Mock/测试/端到端演示/场景全覆盖/性能跨平台/成果/问题/分工/总结） |
| | ✅ 测试：新增 `tests/test_coordinator.py`（24 用例）+ `tests/test_coordinator_agent.py`（3 用例）= **27 新用例**；涉及文件：`core/coordinator.py`（新增）、`core/agent.py`、`config.yaml.example`、`docs/第4组调研报告.md`、`docs/第4组汇报PPT.pptx`（新增）、README / 进度 / 报告素材 |
| 2026-09-10 | **Web 6 场景实测演示（设计书演示场景表，全部通过）** | |
| | ✅ 启动 `examples/miniyu_web.py`（http://localhost:5000），用 `examples/demo_web_scenarios.py` 走 /chat + SSE 链路（与浏览器完全一致），驱动真实 LLM qwen3.7-flash function-calling 逐条执行 6 个场景，**全部一次跑通并经磁盘实体验证** |
| | ✅ 场景 1 打开文件管理器（`explorer`）｜场景 2 导航目录 `list_directory` 列出目标｜场景 3 建文件夹 `project/` 实检存在｜场景 4 文本编辑 read→edit→read 复核实检追加成功｜场景 5 窗口切换 `list_windows→activate_window` 激活 Edge 置前台｜场景 6 整理下载目录：14 步工具链（扫描→按类型建 代码/图片/文本/文档→移动→复核）自主闭环 |
| | ✅ 新增：`docs/第4组Web演示记录.md`（场景表+工具链+核验）、`examples/demo_web_scenarios.py`（可复现）；服务全程无 Traceback、/status 200；README 同步 |
| 2026-09-11 | **安全机制实测 + 危险指令硬拦截接入 Agent 执行链（浏览器验证）** | |
| | ✅ 实测（真实 LLM qwen3.7-flash + Web 界面，最低授权 base 档）：授权下拉显示「基础授权」；「执行 rm -rf /」「格式化 C 盘」→ 直接拒绝无确认弹窗；「执行 shutdown 关机」修复前弹确认窗、**修复后直接拒绝**（对比截图 security_test_3 vs security_test_6） |
| | ✅ 修复：`core/agent.py` 新增 `_sandbox_guard` 接入 `_execute_one`——带 cmd/command 参数的工具命中 DANGEROUS_ACTIONS 绕过确认门硬拦截（杜绝"确认弹窗被误点"的社交工程绕过）；`core/coordinator.py` DANGEROUS_ACTIONS 扩充 Windows/PowerShell 变体（stop-computer / restart-computer / remove-item） |
| | ✅ 新增 `tests/test_agent.py::TestSandboxGuard` 13 例（10 个危险指令参数化 + 正常命令不受影响 + coordinator 关闭保持旧行为），**552 全绿**；新增 `docs/第4组安全机制验证记录.md` + 截图 `docs/evidence/security_test_1~7_*.png`；README 第 4 周计划标记完成、测试数 494→539 更新 |

| 2026-09-11 | **修复确认框反复弹出 + 浏览器默认改用 Edge** | |
| | ✅ 问题定位：确认弹窗"要点好几次确认"的根因是**状态bug**——SSE弹框后，前端点确认并开启轮询，而任务 `status` 一直卡在 `"confirm"`，轮询每次请求都读到 confirm → 反复弹框。修复：确认通过/拒绝后立即把 `status` 重置回 `"pending"`，避免轮询通道重复触发弹窗 |
| | ✅ 浏览器默认：Windows 自动探测优先 Edge 而非 Chrome；新版 Chrome/Edge ≥136 对默认用户档案禁用远程调试，实测独立用户数据目录下 Edge 更稳定；新增配置 `browser.executable`，可显式指定浏览器完整路径（强制固定某款浏览器） |
| | ✅ 测试：自动探测优先返回 Edge 路径；smoke test Edge CDP 启动→导航→关闭清理完整流程通过；现有 558 个单元测试全绿，无回归 |
| | ✅ 文档：`miniyu使用说明.md` 更新说明；`config.yaml` / `config.yaml.example` 新增注释文档；commit 已提交 |

| 2026-09-11 晚 | **浏览器会话隔离 + 窗口前置 + P1 Token/观测成本优化（豆包真实端到端成功）** | |
| | ✅ **前序系列改动**【会话闸门/c首次：A】 `browser_launch/navigate` 后锁 `_browser_active`，浏览器任务期间拦截 `click_at/send_text/send_hotkey/activate_window`（只读工具放行），杜绝"网页任务逃逸到桌面工具"；新增 `browser_refresh`、`browser_bring_to_front` 并在 launch/navigate 自动前置浏览器，用户可见 Agent 操作 |
| | ✅ **持久登录档案**：`config.yaml` `user_data_dir: %LOCALAPPDATA%/miniyu_edge` 独立浏览器档案，首次在某网站登录后 AI 自带登录态，`browser_close` 不清档；`browser_launch` 幂等（同档案已连则复用，防抢 9222 端口）|
| | ✅ **跨平台 3 处修补**：Linux 下整进程组 `killpg` 关闭浏览器（防残留子进程锁 9222）、`launch` 起独立进程组、补 Linux 常见浏览器路径；Windows 走原 taskkill/Path 分支行为不变 |
| | ✅ **过程可观测**：`run_log_*.jsonl` 每行带时间戳的三类记录（llm 想法 / action 操作 / token 用量），密钥字段脱敏；`token_usage` 工具 + 回复末尾附"本轮/本对话/预算"；新增 `tests/test_agent_obs.py`（7 例）→ **590 全绿** |
| | ✅ **P1 观测成本优化**（豆包 9.26 万/27.5 万 token 归因后）：`_MAX_HISTORY_IMAGES=2` 历史图片裁剪 + `_browser_action_needs_visual` 成功结构化动作不自动截图 + System Prompt 第 14 条"结构化读文字"；`reasoning_tokens` 入库 |
| | ✅ **豆包两轮真实 End-to-End 均成功**【二轮】：第一轮账单 274,606 / 图片峰 6；P1 后第二轮账单 **147,362（−46%）**/ 图片峰 **2** / 末轮 prompt 29,284→19,664；`run_log cum=147,362` 与官网账单**完全对账一致**，可作为后续 Benchmark 基准 |
| | ✅ git 提交见当前分支本轮 commit；P1 达成基线 147,362，下一阶段 P2=元素定位可靠性（contenteditable/发送按钮结构化）+ Benchmark |

| 2026-09-12 | **P2 元素定位可靠性 → P2-6 Agent 集成层 + E2E Benchmark 封板（Bean Browser Agent 全链路）** | ✅ |
| | ✅ **P2-2/-3/-4 定位可靠**：stale ref/not_editable→recovery=find、Target Handle 短句柄层、`_resolve_edit_host` 统一可编辑宿主（`2b48687`/`95d61c5`/`b1160bb`/`3df99a1`）；语义 textbox wrapper 下钻到真实可输入节点，`find→type(handle)` 稳定成功 |
| | ✅ **P2-5 语义等待/发现**：`browser_wait_for_change` + anchor 自愈 + `semantic_blocks`(content/control 分类) + `read_latest_reply` 增量（`f629d74`/`6ec58ec`/`c96a826`）；真实豆包会话 WAITING→GENERATING→COMPLETED，只发一次、delta 为真增量 |
| | ✅ **P2-6 Agent 集成层**（`275c477`/`191a190`）：`find→type(handle)→wait_for_change()→read_latest_reply()` 两高层语义工具闭环，`verified_success=true`、`source=structured_read`；全量 667 |
| | ✅ **Benchmark 记账修复（`9dc703f`）**：第二轮 `sum=129188 ≠ final_cum=139633`（差 10445）= **跨运行继承的 LLM 计数器初始值**（首条 llm 行 cum=26766 − 自身 total=16321=10445，实测非倒推），非漏记调用；进入 ReAct 前捕获 `initial_cum_total_used`（run_log `kind='run_start'` 首行），一致性规则改 `sum(recorded)=final−initial`，真实缺口仍判 invalid；全量 667 passed |
| | ✅ **文档封板**：`docs/P2-6-E2E-Benchmark-Report.md`（三轮对比+记账证据）；P2-5 评审稿追加「六、P2-6 集成层+Benchmark 封板」 |
| | ✅ **第三轮冻结 E2E（验证语义 C，`1a2b0fb`）**：修复后重跑（run_log `007801ac/run_log_1789199173138.jsonl`）——`wait_for_change()` 已返回结构化 `message_delta`，Agent 直接作答、**未重复 read**（6 LLM/84,031 tokens，比二轮更省）。verified_success 改判『结构化证据』：`wait_delta`/`latest_reply`→`structured_read` verified，`whole_page`/`model_inference` 不 verified，区分 `verification_path`；wait 返回自声明 `evidence`；对截断 JSON 稳健。**结果：`benchmark_valid=true、verified_success=true、path=wait_delta`，全量 672 passed → P2-6 正式封板** |
| | 🚫 **已封板**：冻结期间不启动新 SoM/image 优化/history 压缩/更多 Browser tools/多网站适配 |
| 2026-09-13 | **gtp 最终验收通过 · Browser Agent Core v1 正式冻结 · 进入跨页面 benchmark 阶段** | ✅ |
| | ✅ **gtp 三轮结论全部认可**：功能、验证语义、账目一致性三条线闭环；验证定义改为「最终回答有无可靠工具结构化证据」，而非「是否机械调用某工具」——正是 benchmark 该测的东西 |
| | ✅ **冻结基线（Core v1 基准，后续所有优化以此对比）**：`LLM calls=6 / run_total=84,031 / benchmark_valid=true / task_success=true / verified_success=true / verification_source=structured_read / verification_path=wait_delta`；调用链 `find→type(handle,enter)→wait_for_change()→复用结构化 message_delta→final`，无 selector fallback / SoM / inspect recovery loop |
| | ✅ **gtp 认可二处设计**：①`wait_delta` 优于强制 `latest_reply`——wait 已返回经 anchor/semantic_blocks/control 过滤/dedup 的语义增量，再强制 read 是「为 benchmark 浪费工具调用」，现在的验证语义规避了反向激励；②wait 自声明 `evidence:{source,verified}` 比「按工具名 if 判断」更成熟，未来可扩展为 `targeted_dom_read / accessibility_text / vision_observation` 按 evidence quality 分级 |
| | ✅ **成本表述更严谨（gtp）**：从 790,014→84,031 下降约 89.4% 是结果之一，但更该写「从 38 个有效 LLM 回合降到 6 个、从数十轮恢复循环变为单次确定性 browser workflow」——真正的架构提升是 **Recovery-driven → Deterministic browser execution** |
| | ✅ **Browser Agent Core v1 定义**：P1 观测成本控制 + P2-3 Target Handle + P2-4 Edit Host Resolution + P2-5 Semantic Observation & Wait + P2-6 Agent Integration（冻结基线见上） |
| | 🚫 **P2-6 冻结**：不改 wait/semantic delta/verification/benchmark accounting/prompt；不加 SoM/图片优化/history 压缩/更多 Browser tools |
| | ✅ **下一阶段（已批复，进入实施）**：最小三类跨页面 benchmark——A 静态页(打开→find→click→read) / B 动态页(点击查询→wait change→read result) / C 聊天流式(豆包：send→wait→delta→answer)，目标是证明 P2-5/P2-6 是**通用 Browser Agent 能力**而非豆包特化 |
| | ✅ **下阶段 benchmark 增加指标（gtp，按优先级进入）**：①`recovery_depth`＝完成任务首次失败后额外产生的 LLM action rounds（0=一次通过；比 action_failures 更接近最初 790k 爆炸根因）②验证拆成 `task_success / verified_success / evidence_quality`（二版 schema，非当下必改） |
| 2026-09-13 | **P2-7 跨页面 Benchmark：规划 + recovery_depth(v1) + 本地 demo + 单测（不执行真实任务）** | ✅ |
| | ✅ **gtp 定案双轨 recovery_depth**：本轮只实现 v1（`core/benchmark.py` 被动推断，不碰 runtime），v2 显式 recovery 事件仅 schema 规划不实现；本轮只做「规划 + 单测」，不跑公共站/不改 P2-6 |
| | ✅ **四类分层 benchmark 设计**：A1 Local Static + A2 Local Dynamic = 正式主集；C 豆包 = Reference 冻结；D Public Web Smoke = 泛化抽查（不进门槛、失败标 `external_smoke_failed`）；四类隔离评价、禁止折总分 |
| | ✅ **recovery_depth v1（heuristic）实现**：`_recovery_metrics()` 按『失败 action → 之后去重 LLM 行数 → 到下一失败/结束』分段计数，产出 `recovery_depth`/`recovery_steps`/`recovery_depth_is_heuristic_v1`；`format_summary` 输出逐失败明细 |
| | ✅ **单测 9 例（`tests/test_recovery_depth.py`）**：gtp 6 边界全过（0/1/多 round、两次分段、立即结束、成功 action 无新 LLM）+ 双写去重不虚增 + 与 token 记账互不污染；全量 **681 passed** |
| | ✅ **本地 demo 雏形（未接 Agent）**：`examples/browser_benchmark/static.html`（button/link/input/select/checkbox/长文本/可展开）、`dynamic.html`（城市查询→1~2s 延迟→结果区文本更新，纯离线非聊天） |
| | 📋 设计文档：`docs/P2-7-跨页面-Benchmark-设计.md`（四类定义 + 冻结边界 + v1/v2 schema）；待 gtp 审核后进 Phase 2（按 A1→A2→C→D 逐个真实执行） |
