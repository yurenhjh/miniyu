4. 第4组：工具注册 + OS Skills 
对应UFO²组件 Tools 
所属层次 工具服务层 
核心职责 工具封装 + 技能定义 
OS原理关联 持久性：文件系统操作、目录管理，直接
体现文件系统原理 
技术栈 Python + shutil + subprocess 
代码量估计 150-200行 
 
（1）功能要求 
• ToolRegistry：管理所有可用工具，提供统一调用接口 
• SkillLibrary：封装系统管理操作（文件整理、系统检查等） 
• 工具签名：验证工具来源和完整性（可选） 
• 调用统计：记录工具调用次数和结果 
• 参考MCP协议设计工具接口 
 
（2）分周进度 
周次 任务内容 
第1周 调研MCP协议，定义接口，实现Mock 
第2周 实现工具注册表，实现文件操作工具 
第3周 实现OS Skills，与第3组联调 
第4周 完善工具签名，参与整体系统集成 
 
（3）代码示例 
# 第4组：工具注册 + OS Skills 
import shutil 
import subprocess 
from pathlib import Path 
 
class ToolRegistry: 
    """工具注册表""" 
    def __init__(self): 
        self.tools = {} 
        self._register_defaults() 
     
    def _register_defaults(self): 
        self.register("copy_file", self._copy_file) 
        self.register("move_file", self._move_file) 
        self.register("run_command", self._run_command) 
     
    def register(self, name: str, func): 
        self.tools[name] = func 
     
    def call(self, name: str, params: dict) -> dict: 
        if name not in self.tools: 
            return {"success": False, "error": "工具未注册"} 
        try: 
            result = self.tools[name](**params) 
            return {"success": True, "result": result} 
        except Exception as e: 
            return {"success": False, "error": str(e)} 
     
    def _copy_file(self, src: str, dest: str) -> str: 
        shutil.copy2(src, dest) 
        return f"已复制: {src} → {dest}" 
     
    def _move_file(self, src: str, dest: str) -> str: 
        shutil.move(src, dest) 
        return f"已移动: {src} → {dest}" 
     
    def _run_command(self, cmd: str) -> str: 
        result = subprocess.run(cmd, shell=True, capture_output=True, text=True) 
        return result.stdout 
 
class SkillLibrary: 
    """OS Skills库""" 
    def organize_downloads(self, path: str = "~/Downloads") -> str: 
        path = Path(path).expanduser() 
        categories = {"文档": [".pdf", ".doc"], "图片": [".jpg", ".png"]} 
        for file in path.glob("*"): 
            if file.is_file(): 
                for cat, exts in categories.items(): 
                    if file.suffix in exts: 
                        (path / cat).mkdir(exist_ok=True) 
shutil.move(str(file), str(path / cat / file.name)) 
return "整理完成" 
if __name__ == "__main__": 
registry = ToolRegistry() 
skills = SkillLibrary()


十、提交要求 
1. 检查点时间表 
检查点 
时间 
第1周结束 
检查点1 
提交材料 
接口文档 + Mock代码 + 
调研报告初稿 
检查点2 
第2周结束 
模块代码 + 设计文档 + 单
元测试报告 
检查点3 
第3周结束 
联调测试报告 + 集成演示
视频 
检查点4 
第4周结束 
完整系统 + PPT汇报 + 最
终报告 
2. 提交格式要求 
（1）报告格式：PDF文档，正文使用宋体小四号字 
（2）代码格式：Python源码（.py），带注释和README 
（3）演示视频：MP4格式，时长不超过5分钟 
（4）PPT格式：PPTX格式，不少于15页 
（5）打包方式：每组提交一个ZIP压缩包，命名为"组号_组长姓名.zip" 
十一、注意事项 
（1）组间协作：各组必须严格遵守接口契约，确保模块能正确集成 
（2）进度对齐：每周检查点必须按时完成，落后组需要及时沟通 
（3）Mock机制：每组必须提供Mock实现，便于其他组独立开发 
（4）安全测试：危险操作必须在安全沙箱中测试，不得在真实系统上执行 
（5）代码规范：代码必须带注释，遵循Python编码规范（PEP8） 
（6）学术诚信：调研报告必须标注参考文献，不得抄袭 
（7）备份机制：代码和文档必须定期备份，防止丢失