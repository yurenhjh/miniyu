# miniyu 桌面 AI 助手 · 使用说明

> 适用：本机已把真实 LLM（阿里云百炼 **qwen3.5-plus**）写进 `config.yaml`，
> 打开即是真机模式，**不需要再设任何环境变量**。密钥与实现细节见 `docs/evidence/agent_qwen_live.md`。

---

## 1. 这是什么

miniyu 是一个 LLM 驱动的桌面 AI 助手：你可以**正常聊天**，也可以**像下命令一样**让它帮你干活。
它会自己规划、调用本机 **59 个系统工具 + 高层组合技能**（文件 / 应用 / 浏览器 / 系统管理 / **直接读本地图片** / **编程项目辅助** / 在 QQ 里**先读会话最近聊天、据实回复** / 发邮件并自动回读核验 / **联网搜索网页**），做完再用中文总结。

> **它能"看见"本地图片了**：给它一个图片路径（如 `D:\桌面\1.3.png` 或直接把图片拖进网页输入框），它会用 `read_image` **直接读取文件**分析——不再需要你先把图片打开在屏幕上靠截屏才能看。主模型有原生视觉就直接看图；主模型无视觉（如部分 DeepSeek）则自动走项目内视觉桥转成文字描述，两种都能识别图里的文字/题目/图表。

> 对模型来说「在 QQ 里找某个群并发消息」是**一个整体技能**（`app_send_message`）：它先在你指定的 QQ 里搜索群名，逐行点开候选会话、用**屏幕 OCR 核对会话标题确实是你说的那个群**，核对无误、并且你在弹窗里点了确认后才会真正发送——不会像对着聊天窗口乱打字那样发到别的会话。
>
> 想让它**先看看群里聊了啥、再据实回一句**？它用的是只读技能 **`read_qq_chat`**：同样先搜群名 + 屏幕 OCR 核对确实是那个群，再**截图裁出聊天消息区、交给视觉桥转写成文字**返回给模型（只读、不发任何消息），然后模型据此调用 `app_send_message` 回复——"先读后回"两步是系统教给它的标准路径，不需要你一步步指导。

> 「发一封邮件」也是**一个整体技能**（`send_email`）：SMTP 发信后它**自动用 IMAP 回读发件箱『已发送』**核验这封信确实发出去并落库；若你在 `config.yaml` 的 `email` 段还配了收件侧邮箱（`verify_inbox`），它还会再核验邮件**确实到达收件人的收件箱**，没验到会如实告诉你，不会只凭“发出去了”就说成功。发信账号与授权码从 `config.yaml` 的 `email` 段读取（本地填、别分享），发信属对外不可撤回动作、会先弹窗征求你同意。
>
> 需要查资料时它也会**联网搜索**：用 `browser_search`（**HTTP 直连必应**，单次约 1 秒内出结果、不开浏览器）搜关键词、再用 `browser_extract` 打开网页读取正文——会告诉你信息来自哪、不会凭空编。

> 问**纯数学计算 / 数据分析**（如「123的21次方是多少？」「算一下这组数的平均值」）时，系统会自动启用**百炼服务端代码解释器**——模型在云端沙箱里写并运行 Python 得出**精确结果**（实测 123 的 21 次方逐位正确，不用它硬算），你直接看结论即可；涉及本地文件/系统操作的提问不受影响，照常走本地工具，两者自动分流、无感切换。

> 让它**改代码 / 查 bug / 理解项目**也能像人一样干活：系统教了它一套「先侦察、后动手」的项目工作流——先用 `list_directory` / `search_files`（按文件名找）/ `search_in_files`（**按内容搜关键词、报错信息、函数名**，支持正则、带行号返回）摸清项目结构和目标代码位置，再用 `read_text_file` 读文件（大文件分段读），理解清楚后才动手；**小改动用 `edit_file` 精确替换**（只改指定原文片段，不整文件重写，更安全），新建文件或大段重写才用 `write_text_file`；改完会用 `run_command` 实际运行验证（如 `python xxx.py`、`pytest`、`git status`），报错就把错误信息喂回搜索定位根因，最后总结改了什么、为什么改。适合"帮我把这个项目里报错的函数修好""这个脚本为什么运行结果不对"这类需求。

两种界面，同一套内核：

| 界面 | 启动命令 | 特点 |
|---|---|---|
| 网页版 | `python examples/miniyu_web.py` | 打开 http://localhost:5000，鼠标操作 |
| 命令行 | `python examples/agent_cli.py` | 终端里打字，命令更全 |

---

## 2. 怎么启动

### 系统与浏览器要求（先看这个）
- **操作系统**：Windows 10/11 或 **Ubuntu 22.04 及以上**（核心代码同一套，双平台通用）。
- **语音输入（🎤 按钮）必须用 Edge 或 Chrome**：Web 界面的语音识别用的是浏览器原生 Web Speech API，只有 **Chrome / Edge** 支持。**正式用推荐 Edge**——它走微软语音服务、国内可用；Chrome 走谷歌服务，大陆常常连不上。用别的浏览器（如 Firefox）时那个按钮会自动隐藏，不影响打字输入。
- **桌面自动化（仅 Linux）需 X11/Xorg 会话**：Ubuntu 默认是 Wayland，`xdotool`/`wmctrl` 这些 X11 工具在 Wayland 下用不了。要用"找窗口/点屏幕/给 QQ 类应用自动化"时，请在登录界面点齿轮 → 选 **「Ubuntu on Xorg」** 再登录；只用文件管理 / Web 聊天则无所谓。
- **网络**：用在线模型（如阿里云百炼）需要能访问对应 API；想完全离线可用本地 Ollama 模型（见下方）。

在项目根目录打开终端，二选一：

```powershell
# 网页版（会自动打开浏览器）
python examples/miniyu_web.py

# 命令行
python examples/agent_cli.py
```

**Linux（Ubuntu）启动**——项目根目录执行：
```bash
# 第一次：一键装依赖+生成 config.yaml（装桌面自动化工具需要 sudo 密码）
bash setup_linux.sh

# 之后每次启动二选一：
bash run_miniyu_web.sh   # 网页版（用 Edge/Chrome 打开）
bash run_miniyu_cli.sh   # 命令行
```

启动后直接说话即可，比如：

> ⚠️ **首次使用 / 把这套代码给别人 / 自己重新部署之前，必看**：
> 1. **换成你自己的 API key 和模型**：仓库里的 `config.yaml` 现在是**开发者的账号配置**，别直接用别人的。打开 `config.yaml` 的 `llm` 段，把 `api_key` 改成你自己的密钥、`base_url` / `model` 配成你要用的服务（格式见 `config.yaml.example`，支持 OpenAI 兼容的任何服务）。没填 key 也能启动，但只会进**离线模式**（只认预设指令、不能真对话）。
> 2. **想用本地模型？得自己下载**（见下方「可选：使用本地 Ollama 模型」）。
> 3. 填好后重启（Ctrl+C 停、再 `python examples/miniyu_web.py`）。

---

### 可选：使用本地 Ollama 模型（纯离线，不花 API 流量）

项目额外支持接本地模型做**日常聊天/写作**（不走云端 API，完全离线）。本地模型是**你自己下载的**，项目不内置、不替你下载。步骤如下：

1. **下载安装 Ollama**：到 [ollama.com](https://ollama.com) 下载安装包装好（装完通常会在托盘常驻）。国内直连慢的话，可以挂梯子或用镜像，也可以到 GitHub Releases 手动下载安装包。
2. **确认能跑**：命令行执行 `ollama --version` 有输出版本即装好。
3. **拉一个本地模型**（按需选一个，别全拉，很占磁盘）：
   ```powershell
   ollama pull huihui_ai/qwen2.5-abliterate:1.5b     # 推荐日常　≈1.1GB，首字约 4s
   # 或带思考过程的：
   ollama pull huihui_ai/qwen3-abliterated:4b        # ≈2.3GB，有"已深度思考"展示，稍慢
   ```
4. （可选）**把模型文件存到非系统盘**：提前设环境变量 `OLLAMA_MODELS` 指向 D 盘等（如 `D:\ollama_models`），再拉模型就不会占 C 盘。不设置默认在 C 盘用户目录。
5. **用之前要先把 Ollama 跑起来**（它是个后台服务，不跑网页就找不到本地模型），三种做法任选其一：
   - **最省事（推荐）**：双击桌面的 **Ollama** 快捷方式 / 从开始菜单打开。它会启动并**静默常驻桌面右下角托盘**，之后一直服务、无需再管（开机也会自动起；不想随开机启动，可在 Ollama 设置里关掉「Launch at Login」）。
   - **命令行**：开一个终端执行 `ollama serve`。注意这是**前台常驻**——这个终端窗口不能关，关了就退出。适合临时用、或你想只在这段时间开。
   - **确认已起**：任意终端敲 `ollama list`，能列出已下载的模型就说明服务正常；或看右下角托盘有没有 Ollama 图标。
   > 判断「到底要不要手动启动」：miniyu 网页每次打开都会自动探测本地 Ollama——**探测到在运行** → 顶栏就会出现 🖥️ 本地模型；**没探测到** → 只有 ☁️ 在线模型，此时才需要按上面第 5 步把它启动。
6. 启动 miniyu 网页版，顶栏**模型搜索框** → **🖥️ 本地 Ollama 模型** 分组下拉就能看到刚拉的模型，点选即切换到本地离线模式；想回云端就在同一搜索框任选一个 ☁️ 在线模型。

> 本地模型只做**日常对话/写作**，不做工具/文件/QQ/联网（CPU 解析工具太慢，见 FAQ）。装好但没在网页里看到？确认 Ollama 托盘在运行（没运行就点开托盘 Ollama 或执行 `ollama serve`）。

- 「在桌面建一个叫 会议纪要 的文件夹」
- 「把 C:\下载 按类型整理一下」
- 「写个文件 C:\笔记\待办.txt，内容是：买牛奶、交房租」
- 「找一下桌面上超过 100MB 的大文件」
- 「现在磁盘还剩多少空间？」
- 「打开记事本」/「用浏览器搜一下 qwen3.5」
- 「在 QQ 里找到群『一中兄弟会之大压抑时代』，发一句 你好」
- 「给 3837227570@qq.com 发一封邮件，主题是『测试』，正文写：你好呀，小余人」（需先配好邮箱授权码）

**高危动作**（删除文件/文件夹、结束进程、外发消息、发邮件等）默认会先征求你同意：
- 网页版弹确认框，点允许/拒绝；
- 命令行问 `是否执行？(y/n)`，输 `y` 放行、`n` 取消。

> **授权档位**（`agent.authorization`，Web 顶栏下拉 / CLI `/authz` 可切，默认 `base`）决定哪些高危动作要征求同意：
> - **base 基础授权**：以上全部高危动作都先确认；
> - **advanced 高级授权**：只有**删除文件**类（删文件/文件夹、按类型批量删、清空回收站）先确认，执行命令/杀进程/QQ 发消息/发邮件自动放行；
> - **full 全自动**：一律不确认（含文件删除）。网页版切到「全自动」会先弹一次浏览器确认把关。
> QQ 发送的屏幕 OCR 核对门不随档位关闭——自动放行不等于会发错群。

> 想临时切回**离线脑**（不联网、只认预设指令）：把 `config.yaml` 里 `provider: openai_compatible` 改成 `deterministic` 即可。

---

## 3. 命令行快捷键（/命令）

在 `agent_cli.py` 里输入以下命令：

| 命令 | 作用 |
|---|---|
| `/help` | 显示帮助 |
| `/model` | 看当前模型（真机应显示 qwen3.5-plus） |
| `/authz` | 查看 / 切换授权档位：`/authz base|advanced|full`（无参查看当前） |
| `/tools` | 列出全部 59 个可用工具 |
| `/config` | 打印当前配置（会含 key，注意别截图外发） |
| `/reset` | 清空**当前会话**的消息（会留一个空会话记录） |
| `/new` | 新建一个会话（对标「新对话」） |
| `/sessions` | 列出所有历史会话 |
| `/switch <id>` | 切到指定会话 |
| `/rename <id> <新标题>` | 给会话改名 |
| `/delete <id>` | **删除某个会话（连带删记录文件）**，不能删当前会话，先 `/switch` 走 |
| `/exit` | 退出 |

---

## 4. 网页版按钮

- 左侧 **+ 新对话**：开新会话
- **历史会话自动命名**：每个会话的标题会自动取你**第一句话的第一句**（首个逗号/句号前的内容，超 20 字截断加 …），所以侧栏一眼就能看出每段对话是聊什么的；旧会话里仍显示"新对话"的，加载时也会自动用首条消息补上标题
- 会话侧栏每条右侧 **×**：删除该会话（会先弹确认，**连带删记录文件**）
- 点会话条目：切换历史对话
- 顶部 **重置**：清空当前会话内容（= `/reset`，会留一个空会话记录）
- 底部 **⏹ 停止**（生成中才出现）：回复生成期间「发送」按钮会变成红色「⏹ 停止」，点它可**立即打断**当前输出（本地小模型偶发死循环/超长输出时的救命键，和 DeepSeek 官网的停止同款）。打断后已输出的部分文字会保留，末尾加「⏹ 已手动停止生成。」标记，可直接继续发下一句
- 输入框左侧 **🎤 语音输入**（仅 Chrome/Edge 显示）：点一下开始倾听（按钮变**红色脉动**、输入框提示"正在倾听"），**再点一下关闭**；**持续倾听**——说话中间停顿不会结束，你说完点 🎤 才关闭；识别出的文字**实时填入输入框**（接着已有内容**追加**，多次语音输入/手动打字自然累积成一段），确认无误后按 Enter 或点「发送」提交。适合不方便打字时把指令"说"给 miniyu：
  - 原理：用**浏览器原生的语音识别**（Web Speech API），**零后端、零安装**，local 调试不需要 HTTPS；
  - 注意：识别由浏览器厂商的云端服务完成（**不是本地**、也不是 miniyu 发的）——Chrome 走谷歌服务（大陆网络常连不上，会红条提示"浏览器语音服务不可用"），**Edge 走微软服务、国内可用，建议用 Edge 开网页版**；
  - 隐私提醒：说话内容会上传到你浏览器的语音服务商做识别，敏感内容建议打字。若不想被识别/不支持（Firefox/Safari 不显示按钮），直接打字即可，功能完全不受影响
- 顶栏**模型搜索框**（可搜索下拉框）：点击输入关键词，**输入即实时过滤**（像浏览器地址栏那样，不用输全名就能看到候选），选项按 **☁️ 在线 API 模型** 和 **🖥️ 本地 Ollama 模型** 分组：
  - 选**在线模型**：点选即切换，切换**会先实测连通**——成功弹绿色提示「切换成功可正常使用」，失败弹红色提示原因（当前模型保持不变）。选择会写进 `config.yaml` 的 `model`，重启后仍生效；
  - 选**本地模型**（需本机已装 [Ollama](https://ollama.com) 并拉好模型）：点选后**对话直接改用该本地模型**（跳过云端 API，完全离线跑），切回任一在线模型即恢复云端。可用本地模型自动从 Ollama 读取（`ollama list` 有什么就列什么），候选清单也在 `config.yaml` 的 `llm.fallback.models` 里维护；
  - 键盘：**Enter** 选中首个匹配项、**Esc** 关闭下拉、失焦自动收起；
  - 离线/未接 API 时在线分组禁用并提示先接入。
  > 小知识：换模型后**聊天历史不丢**——系统会自动把旧模型留下的工具调用记录转成普通文字，所以切到任何模型（哪怕不支持工具调用格式）都能接着聊。
- 顶栏**授权档位下拉**（基础授权 / 高级授权 / 全自动）：决定哪些高危操作要确认，点选即切换（写进 `config.yaml` 的 `agent.authorization`，重启后仍生效），选「全自动」会先弹一次浏览器确认把关
- 顶栏 **☀️/🌙 主题按钮**（在「会话: N」徽章的左边）：一键切换**深色/浅色**主题，选择会记住（`localStorage`），刷新/下次打开自动保持；默认深色
- **工具调用折叠面板**：模型干活时调用的工具（文件操作/搜索/QQ 等）集中显示成一个面板，**默认折叠为一行**「工具调用 (N 个) ▼」——点标题**展开**、再点**收起**，展开后内容**限高、超出可在框内滚动**查看（和「已深度思考」面板同款交互）——既不占地方，又能随时看清它每一步干了什么
- **页面整体固定在窗口里**：滚动只发生在聊天区内部，整个界面不会被划出浏览器窗口（页面本身不能拖动/横向滑动）
- **数学公式与 Markdown 正常显示**：模型回复里的 `$...$`（行内公式，如 `$A^2-3A-2E=O$`）和 `$$...$$`（块级公式）会自动渲染成**正常书写的数学排版**（上标、分式、根号、求和号等），不会再显示一串 `$` 和反斜杠；`**加粗**`、`` `代码` ``、列表、表格等 Markdown 也按惯例显示。渲染用的 KaTeX/marked/DOMPurify 已随项目**本地打包**（`examples/static/vendor/`），不依赖外网 CDN，断网也能正常渲染

---

## 5. 聊天记录存在哪、怎么删（重点）

### 存在哪
你的每次对话都会自动存档，位置在项目根目录：

```
conversations\
    会话ID.json     ← 一个会话一个文件，里面就是你们的对话
```

- 每聊完一轮自动写入；下次启动自动读回，所以关掉再开历史还在。
- 存的是**本机明文 JSON**，肉眼能看。

### 怎么删单条会话（推荐，边删边生效）
- **网页版**：会话列表里点要删那条的 **×** → 确认。文件会一并删除。
- **命令行**：`/sessions` 看列表 → `/switch` 切到别的会话 → `/delete <目标ID>`。

> 「重置 / reset」≠ 删除：它只清空当前会话的聊天内容，**仍会留下一个空会话文件**。
> 想彻底不留，用上面的 × 或 /delete。

### 想一次性全部清空
1. **先退出程序**（miniyu 没关时可能把会话再写回磁盘）；
2. 删除 `conversations` 目录里所有 `*.json`（目录本身可留）；
3. 下次启动就是全新空白，不报错。

命令行一条清空：

```powershell
Remove-Item .\conversations\*.json
```

### 想让项目里根本不落任何记录
把 `config.yaml` 的会话目录指到临时区，例如：

```yaml
memory:
  storage_dir: "C:\Users\你的用户名\AppData\Local\Temp\miniyu_sessions"
```

这样记录写在系统临时目录（重启/清理时会被系统回收），项目文件夹始终干净。

### 截图 / 过程产物存哪、怎么清（重要）
miniyu 干活过程中为“看屏幕理解情况”自动截的图等**过程产物，单独放、不混进聊天记录**，
每会话一个子目录：

```
%TEMP%\miniyu_artifacts\<会话ID>\shots\    ← 默认位置（可用 AGENT_ARTIFACTS_DIR 或
                                              config.yaml 的 memory.artifacts_dir 改）
```

它们只是过程留档，方便回看与后续技能接力复用。想清掉，**直接说人话**：

- 对 miniyu 说 **「清理截图」/「清理产物」/「清空截图」** → 只清当前会话的过程产物；
- 或 **`/reset`**（网页版「重置」）→ 会话消息**和**当前会话过程产物一起清空。

> 注意：网页版/命令行里模型为了确认界面状态会截屏并**把图片发给视觉模型**看——所以「让它盯屏幕干活」的
> 场景会把截图（含可能出现在屏幕上的内容）发送到云端，演示时留意屏幕别放敏感东西。

### 两句提醒（重要）
1. 你发的每句话会**联网发给阿里云百炼**去生成回复。删掉本机 JSON ≠ 云端没有——**别在演示里发敏感信息**。
2. `config.yaml` 里是**明文 API key**。项目不是 git、纯本地没事；但**别把整个文件夹发给别人 / 交作业打包前记得删掉 key 行**。

---

## 6. 常见问题

| 现象 | 处理 |
|---|---|
| 感觉还是离线脑？ | 输入 `/model`：显示 `qwen3.5-plus` 才是真机；`deterministic` 就是离线 |
| 报「出错了 / 网络 / 额度」 | 检查网络；到阿里云百炼控制台看 key 是否有余额/额度（本机不承诺免费） |
| 想让演示不联网 | `config.yaml` 把 provider 改回 `deterministic`，或设 `AGENT_LLM_PROVIDER=deterministic` |
| 换模型/换 key | 网页版顶栏**模型搜索框**点选即可（在线模型自动实测连通，成功/失败都有提示）；或编辑 `config.yaml` 的 `llm` 段（base_url / api_key / model） |
| 切了本地模型怎么没走云端了？ | 这是**预期行为**：选本地 Ollama 模型＝主动切到本地离线模式（跳过云端 API），想回云端就在模型搜索框里任选一个 ☁️ 在线模型。本地模型跑 CPU 会慢一些（7B 约 30 秒/条），属正常 |
| 本地模型怎么不能整理文件/发 QQ 了？ | 这是**设计如此**：本地模型走**纯对话模式**（`tool_free`）——不带 66 个工具定义（纯 CPU 光解析工具描述就要 30~150 秒，太慢），只做日常聊天、写作、解释概念。涉及文件操作/QQ/邮件/联网，回复里它自己会建议你切回在线模型。写长文（如故事）也不受总超时限制（流式输出），实测 7B 写三句话小故事约 1 分钟 |
| 本地模型回复变乱码（`æ°è½æº` 这种）？ | 已修（09-07）。根因不是模型：Ollama 流式响应头不带 charset，旧代码按 ISO-8859-1 误解码 UTF-8 中文。现在按字节读流 + UTF-8 增量解码，三个本地模型实测全部正常中文。若你在这天之前的旧网页标签页里仍看到乱码，**刷新一下页面**（F5）即可——旧标签页跑的是旧版前端脚本 |
| 模型陷入死循环/一直输出停不下来？ | 点输入框旁红色 **⏹ 停止** 按钮即可秒级打断（详见第 4 节）。实测从点击到停止生效 <0.5 秒，已输出内容不丢 |
| 网页版怎么有时能看到"思考中"的滚动内容？ | 本地 qwen3 模型带独立思考通道：生成时**实时滚动显示思考过程并计时**，完成后折叠成「已深度思考（用时 Xs）」，点 ▼ 可展开回看——和 DeepSeek 官网同款体验。无独立思考通道的模型（如 qwen2.5）则没有这一段，直接出正文，属正常 |
| 切完模型对话报错 / 历史不兼容？ | 已修：换模型时系统自动清理历史里的工具调用格式消息（不支持 function calling 的模型也能无缝接力历史）。若仍报错，先点「重置」清空当前会话再试 |
| 它在 QQ 里发消息会不会发错群？ | 必须走 `app_send_message` 组合技能：先搜群名 → 逐行**屏幕 OCR 核对会话标题** → 弹窗确认 → 才发送；核对不过或你点拒绝就**不会发**。前提是桌面 QQ 已登录且开着 |
| 它“看不到聊天历史”、不会变通？ | 让它先 **读** 再 **回** 它就会了：`read_qq_chat`（只读：搜群名 + OCR 核对标题 + 截图转写聊天区为文字）→ 再 `app_send_message` 回复。读的是**最近可见的聊天**，只读不发；没 OCR/视觉源时会如实报错、不会假装读到 |
| 发邮件怎么保证真发出去、不发错？ | 必须走 `send_email` 组合技能：SMTP 发信后**自动用 IMAP 回读**发件箱『已发送』核验已落库；`config.yaml` 的 `email` 段再配 `verify_inbox`（收件侧邮箱的 IMAP）还会核验**确实到达收件人收件箱**；回读没验到会如实说。前提：email 段已填你的邮箱账号 + **授权码**（QQ 邮箱『设置→账户→开启 SMTP/IMAP 服务』生成，不是登录密码；SMTP/IMAP 共用一把；别把授权码发给别人/别提交进仓库） |
| 它能联网搜资料吗？ | 真实 LLM 模式下可以：用 `browser_search`（HTTP 直连必应，默认 bing；百度有"安全验证"墙所以未接入）搜关键词、`browser_extract` 打开网页读正文，回复会**标注来源、不编造**。离线脑（`deterministic`）不联网，只认预设指令 |
| 之前联网搜卡好几分钟还报超时，修了吗？ | 修了。根因是旧实现用无头浏览器"打字+回车"，在必应/百度都不触发跳转、每次卡满 10 秒超时，模型重试几次就堆到几分钟。现在 `browser_search` 是 **HTTP 直连必应结果页**再解析，实测单次约 1 秒内返回前几条 {标题/链接/摘要}。它若觉得结果不够贴题，会换关键词再搜，或调 `browser_extract` 打开某条链接细读 |
| 它怎么“看懂”屏幕、出错时会不会截图？ | 模型做 GUI 操作后会自己截屏看效果；拿不准时也可先截屏再继续（能力函数 `screen_inspect`）。**看图/OCR 用谁只看 `config.yaml` 的 `llm.supports_vision`**：当前 qwen3.5-plus **有原生视觉**（`true`）→ 截图直接发给它看；若日后把主对话换成**无视觉**的模型（如部分 DeepSeek），把它改回 `false` → miniyu 自动改走**项目内视觉桥** `core/vision_bridge.py`（读 config.yaml 的 `vision_bridge` 段，独立于主对话的第二个视觉 API）把图转文字。两种填法都能全功能，模板见 `config.yaml.example` |
| 截图过程产物越来越多？ | 直接说 **「清理截图」/「清理产物」**，或 `/reset`（网页版「重置」），会清空当前会话的截图等过程产物（见第 5 节） |
| 它能直接读本地图片吗（不用先打开/截图）？ | 能。把图片路径告诉它（如「看看 D:\图片\1.3.png 里的题目」），或把图片文件拖进网页输入框，它会用 `read_image` **直接读文件分析**。读图效果取决于视觉源：`config.yaml` 的 `llm.supports_vision: true`（当前 qwen3.5-plus）→ 主模型直接看图；`false` → 自动走 `vision_bridge` 段配置的独立视觉 API 转文字。两个源都没配好时会明确报错提示，不会瞎编 |
| 让它改代码/查 bug 能像人一样干活吗？ | 能。系统教了它「先侦察、后动手」工作流：`search_files`（按文件名）+ `search_in_files`（**按内容搜关键词/报错/函数名**，支持正则、带行号）+ `read_text_file` 摸清结构 → **`edit_file` 精确局部替换**（不整文件重写）→ `run_command` 实际运行验证。适合"这个脚本为什么报错""帮我修好某个函数""这个项目结构是啥样" |
| 数学公式显示成一串 `$` 和反斜杠？ | 已修（09-08）：前端现在用 **KaTeX** 把 `$...$`/`$$...$$` 渲染成正常数学排版，Markdown 用 marked 解析、DOMPurify 消毒，库已本地打包（`examples/static/vendor/`）不依赖外网。若你在这天之前的旧标签页里仍看到 `$` 原文，**硬刷新（Ctrl+F5）**即可 |
| 主题色能换吗？ | 能。顶栏「会话: N」徽章左边的 **☀️/🌙** 按钮一键切深/浅色，选择自动记住，下次打开保持 |
| 问数学计算题（如 123 的 21 次方）它会算准吗？ | 纯计算/数据分析题会自动启用**百炼服务端代码解释器**——模型在云端沙箱里写并运行 Python，返回**逐位精确**结果（实测 123²¹ 全对），不再硬算/乱报。前提：用百炼在线模型、且 `config.yaml` 的 `web_search.code_interpreter` 保持开（默认开，不需要可在该文件关掉省 token）。模型不支持时系统自动探测停用、照常回答；涉及本地文件/系统的提问不受影响，照常走本地工具 |
| 工具调用显示太多、占屏幕？ | 已改为**折叠面板**（09-08）：默认只显示一行「工具调用 (N 个) ▼」，点开才展开详情（限高、超出可滚动），看完再点收起——不占输出区，又能审计模型干了什么 |
| 整个页面能被划出窗口/横向拖动？ | 已修（09-08）：页面整体固定在浏览器窗口里，滚动只发生在聊天区内部，不会再出现整页被划出/左右滑动 |
| 网页里没有 🎤 语音按钮？ | **语音输入只用浏览器原生 Web Speech API，只有 Chrome / Edge 支持**。推荐 Edge（走微软语音、国内可用），Chrome 走谷歌服务大陆常连不上。用 Firefox 等其它浏览器会自动隐藏该按钮，打字输入不受影响 |
| 点了 🎤 没声音/没字？ | ① 先确认你有麦克风、点地址栏左侧 🔒 授予麦克风权限；② 语音识别依赖浏览器云端服务，`network` 错误多因网络/服务不可用，可**换 Edge** 或直接打字；③ 它是**持续倾听**，说话停顿不结束，要点 🎤 二次点击才关闭并上屏，别以为卡住了 |
| 在 Linux（Ubuntu）上怎么用？ | ① `bash setup_linux.sh` 一键装依赖+生成配置；② `bash run_miniyu_web.sh` 或 `run_miniyu_cli.sh` 启动；③ 用桌面自动化（找窗口/点击/截图）要先在登录界面选 **「Ubuntu on Xorg」**（xdotool 是 X11 工具，默认 Wayland 会话不可用），仅文件管理/聊天不受影响 |
| 想把这份代码发给同学/换台电脑用？ | 见第 7 节「给别人部署」——核心就一句：`cp config.yaml.example config.yaml` 后填**他自己的** key；开发者的 key 和本地模型不随代码分发 |

---

## 7. 给别人部署（换机器 / 发给同学）

**这是一份"个人钥匙不在代码里、谁都能配成自己可用的"项目。** 你本地那份 `config.yaml` 里是你的 API key / 邮箱授权码 / 本地模型名，**不要**把它发给别人；发出去的是代码 + 空模板，对方自己填自己的即可完整使用。

**对方拿到后（Windows / Ubuntu 通用，约 4 步）：**

```bash
# 1）装依赖（Python 3.10+）
pip install -r requirements.txt

# 2）从模板生成自己的配置，并填自己的 key（关键一步）
cp config.yaml.example config.yaml      # Windows 用: copy config.yaml.example config.yaml
#    编辑 config.yaml：
#      · llm.api_key / llm.model —— 必填，你自己的主对话 API（百炼/OpenAI/DeepSeek 皆可，格式见模板）
#      · llm.supports_vision / vision_bridge —— 主模型无视觉时再填一段独立视觉 API
#      · email.* —— 只用“发邮件”才需要（授权码是敏感字段，自己填、别分享）
#      · fallback.* —— 想免费离线降级就装 Ollama 并填本地模型（见第 2 节）
#      · web_search.enabled —— 联网/百炼代码解释器开关，按自己需求保留

# 3）Linux 桌面自动化额外装系统工具（可选；Windows 跳过）
bash setup_linux.sh        # 装 xdotool/wmctrl/xclip/gnome-screenshot + 生成配置

# 4）启动
python examples/miniyu_web.py    # Windows 也双击 run_miniyu_web.bat；Linux 用 bash run_miniyu_web.sh
```

**没填 key 直接启动**会进**离线模式**（只认预设指令、不能真对话），一样能跑通全流程做演示。

**三句提醒：**
1. `api_key` / 邮箱 `auth_code` 是敏感字段，**只在你本地填、绝不 commit / 发群**——项目 `.gitignore` 和 git 的 skip-worktree 已帮你兜底，但你自己也要注意别把改好的 `config.yaml` 打包发出去。
2. 想用本地模型降级，**对方要自己下载**：`ollama pull <模型名>`（本地模型不出现在你发的包里）。
3. `conversations/`（聊天记录）和 `%TEMP%/miniyu_artifacts`（过程产物）也是本地生成的，不在包里。

---

## 8. 目录速查

| 路径 | 是什么 |
|---|---|
| `examples/agent_cli.py` | 命令行入口 |
| `examples/miniyu_web.py` | 网页版入口 |
| `examples/live_llm_check.py` | 真机自检驱动（跑一遍证明 LLM↔文件系统通） |
| `examples/email_demo.py` | 邮件全链路演示（发一封并自动回读核验，`--agent` 走真实 LLM） |
| `config.yaml` | 真机/离线切换、模型、key、会话目录、**email 段**（发件账号+授权码） |
| `conversations/` | 你的聊天记录（想删看第 5 节） |
| `core/` | 内核：LLM 客户端、Agent 编排、59 工具、对话记忆 |
| `core/email_client.py` | 邮件协议层（纯 stdlib：SMTP 发信 + IMAP 回读核验） |
| `core/vision_bridge.py` | 视觉桥：主模型无视觉时把图片/截图转成文字描述（独立第二视觉 API） |
| `examples/static/vendor/` | 前端本地打包的第三方库（KaTeX/marked/DOMPurify，离线可渲染公式与 Markdown） |
| `docs/evidence/agent_qwen_live.md` | 真实 LLM 联调的证据记录 |
| `%TEMP%\miniyu_artifacts\` | 截图等过程产物（默认；想清看第 5 节） |

---

*最后整理：2026-09-09 · 第4组 miniyu*
